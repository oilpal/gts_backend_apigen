/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.am;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.am.domain.AmCodeProject;
import com.neofect.gts.core.services.am.service.AmCodeProjectService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link AmCodeProject}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class AmCodeProjectResource {

    private final AmCodeProjectService amCodeProjectService;

    public AmCodeProjectResource(AmCodeProjectService amCodeProjectService) {
        this.amCodeProjectService = amCodeProjectService;
    }

    //region ------------------------------------------------------------------------------------------------ amCodeProject API

    /**
     *
     */
    @GetMapping(value = "/am-code-project", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<AmCodeProject>> listAmCodeProject(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<AmCodeProject> amCodeProjectList = amCodeProjectService.listAmCodeProject(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(amCodeProjectList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/am-code-project/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = amCodeProjectService.listAmCodeProjectCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/am-code-project/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeProject> getEmptyAmCodeProject() {
        AmCodeProject amCodeProject = new AmCodeProject();
        return new ResponseEntity<>(amCodeProject, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/am-code-project/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeProject> getAmCodeProject(@PathVariable("id") Long id) {
        AmCodeProject amCodeProject = amCodeProjectService.getAmCodeProject(id);
        return new ResponseEntity<>(amCodeProject, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/am-code-project")
    public ResponseEntity<Void> addAmCodeProject(@RequestBody AmCodeProject amCodeProject) {
        amCodeProjectService.saveAmCodeProject(amCodeProject);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/am-code-project/{id}")
    public ResponseEntity<Void> updateAmCodeProject(@PathVariable("id") Long id, @RequestBody AmCodeProject amCodeProject) throws Exception {
        // if (!id.equals(amCodeProject.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        amCodeProjectService.saveAmCodeProject(amCodeProject);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/am-code-project/{id}")
    public ResponseEntity<Void> removeAmCodeProject(@PathVariable("id") Long id) {
        amCodeProjectService.removeAmCodeProject(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
