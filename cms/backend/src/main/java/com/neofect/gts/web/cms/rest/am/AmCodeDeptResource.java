/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.am;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.am.domain.AmCodeDept;
import com.neofect.gts.core.services.am.service.AmCodeDeptService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link AmCodeDept}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class AmCodeDeptResource {

    private final AmCodeDeptService amCodeDeptService;

    public AmCodeDeptResource(AmCodeDeptService amCodeDeptService) {
        this.amCodeDeptService = amCodeDeptService;
    }

    //region ------------------------------------------------------------------------------------------------ amCodeDept API

    /**
     *
     */
    @GetMapping(value = "/am-code-dept", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<AmCodeDept>> listAmCodeDept(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<AmCodeDept> amCodeDeptList = amCodeDeptService.listAmCodeDept(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(amCodeDeptList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/am-code-dept/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = amCodeDeptService.listAmCodeDeptCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/am-code-dept/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeDept> getEmptyAmCodeDept() {
        AmCodeDept amCodeDept = new AmCodeDept();
        return new ResponseEntity<>(amCodeDept, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/am-code-dept/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeDept> getAmCodeDept(@PathVariable("id") Long id) {
        AmCodeDept amCodeDept = amCodeDeptService.getAmCodeDept(id);
        return new ResponseEntity<>(amCodeDept, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/am-code-dept")
    public ResponseEntity<Void> addAmCodeDept(@RequestBody AmCodeDept amCodeDept) {
        amCodeDeptService.saveAmCodeDept(amCodeDept);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/am-code-dept/{id}")
    public ResponseEntity<Void> updateAmCodeDept(@PathVariable("id") Long id, @RequestBody AmCodeDept amCodeDept) throws Exception {
        // if (!id.equals(amCodeDept.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        amCodeDeptService.saveAmCodeDept(amCodeDept);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/am-code-dept/{id}")
    public ResponseEntity<Void> removeAmCodeDept(@PathVariable("id") Long id) {
        amCodeDeptService.removeAmCodeDept(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
