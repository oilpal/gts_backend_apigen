/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.am;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.am.domain.AmCodeKind;
import com.neofect.gts.core.services.am.service.AmCodeKindService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link AmCodeKind}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class AmCodeKindResource {

    private final AmCodeKindService amCodeKindService;

    public AmCodeKindResource(AmCodeKindService amCodeKindService) {
        this.amCodeKindService = amCodeKindService;
    }

    //region ------------------------------------------------------------------------------------------------ amCodeKind API

    /**
     *
     */
    @GetMapping(value = "/am-code-kind", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<AmCodeKind>> listAmCodeKind(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<AmCodeKind> amCodeKindList = amCodeKindService.listAmCodeKind(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(amCodeKindList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/am-code-kind/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = amCodeKindService.listAmCodeKindCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/am-code-kind/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeKind> getEmptyAmCodeKind() {
        AmCodeKind amCodeKind = new AmCodeKind();
        return new ResponseEntity<>(amCodeKind, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/am-code-kind/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeKind> getAmCodeKind(@PathVariable("id") Long id) {
        AmCodeKind amCodeKind = amCodeKindService.getAmCodeKind(id);
        return new ResponseEntity<>(amCodeKind, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/am-code-kind")
    public ResponseEntity<Void> addAmCodeKind(@RequestBody AmCodeKind amCodeKind) {
        amCodeKindService.saveAmCodeKind(amCodeKind);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/am-code-kind/{id}")
    public ResponseEntity<Void> updateAmCodeKind(@PathVariable("id") Long id, @RequestBody AmCodeKind amCodeKind) throws Exception {
        // if (!id.equals(amCodeKind.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        amCodeKindService.saveAmCodeKind(amCodeKind);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/am-code-kind/{id}")
    public ResponseEntity<Void> removeAmCodeKind(@PathVariable("id") Long id) {
        amCodeKindService.removeAmCodeKind(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
