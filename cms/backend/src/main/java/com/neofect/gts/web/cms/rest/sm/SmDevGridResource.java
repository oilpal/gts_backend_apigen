/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.sm;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.sm.domain.SmDevGrid;
import com.neofect.gts.core.services.sm.service.SmDevGridService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link SmDevGrid}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class SmDevGridResource {

    private final SmDevGridService smDevGridService;

    public SmDevGridResource(SmDevGridService smDevGridService) {
        this.smDevGridService = smDevGridService;
    }

    //region ------------------------------------------------------------------------------------------------ smDevGrid API

    /**
     *
     */
    @GetMapping(value = "/sm-dev-grid", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<SmDevGrid>> listSmDevGrid(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<SmDevGrid> smDevGridList = smDevGridService.listSmDevGrid(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(smDevGridList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/sm-dev-grid/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = smDevGridService.listSmDevGridCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/sm-dev-grid/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmDevGrid> getEmptySmDevGrid() {
        SmDevGrid smDevGrid = new SmDevGrid();
        return new ResponseEntity<>(smDevGrid, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/sm-dev-grid/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmDevGrid> getSmDevGrid(@PathVariable("id") Long id) {
        SmDevGrid smDevGrid = smDevGridService.getSmDevGrid(id);
        return new ResponseEntity<>(smDevGrid, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/sm-dev-grid")
    public ResponseEntity<Void> addSmDevGrid(@RequestBody SmDevGrid smDevGrid) {
        smDevGridService.saveSmDevGrid(smDevGrid);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/sm-dev-grid/{id}")
    public ResponseEntity<Void> updateSmDevGrid(@PathVariable("id") Long id, @RequestBody SmDevGrid smDevGrid) throws Exception {
        // if (!id.equals(smDevGrid.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        smDevGridService.saveSmDevGrid(smDevGrid);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/sm-dev-grid/{id}")
    public ResponseEntity<Void> removeSmDevGrid(@PathVariable("id") Long id) {
        smDevGridService.removeSmDevGrid(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
