/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.member;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.member.domain.MemberClock;
import com.neofect.gts.core.services.member.service.MemberClockService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link MemberClock}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class MemberClockResource {

    private final MemberClockService memberClockService;

    public MemberClockResource(MemberClockService memberClockService) {
        this.memberClockService = memberClockService;
    }

    //region ------------------------------------------------------------------------------------------------ memberClock API

    /**
     *
     */
    @GetMapping(value = "/member-clock", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<MemberClock>> listMemberClock(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<MemberClock> memberClockList = memberClockService.listMemberClock(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(memberClockList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/member-clock/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = memberClockService.listMemberClockCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/member-clock/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<MemberClock> getEmptyMemberClock() {
        MemberClock memberClock = new MemberClock();
        return new ResponseEntity<>(memberClock, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/member-clock/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<MemberClock> getMemberClock(@PathVariable("id") Long id) {
        MemberClock memberClock = memberClockService.getMemberClock(id);
        return new ResponseEntity<>(memberClock, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/member-clock")
    public ResponseEntity<Void> addMemberClock(@RequestBody MemberClock memberClock) {
        memberClockService.saveMemberClock(memberClock);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/member-clock/{id}")
    public ResponseEntity<Void> updateMemberClock(@PathVariable("id") Long id, @RequestBody MemberClock memberClock) throws Exception {
        // if (!id.equals(memberClock.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        memberClockService.saveMemberClock(memberClock);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/member-clock/{id}")
    public ResponseEntity<Void> removeMemberClock(@PathVariable("id") Long id) {
        memberClockService.removeMemberClock(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
