/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.am;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.am.domain.AmCodeDeposit;
import com.neofect.gts.core.services.am.service.AmCodeDepositService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link AmCodeDeposit}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class AmCodeDepositResource {

    private final AmCodeDepositService amCodeDepositService;

    public AmCodeDepositResource(AmCodeDepositService amCodeDepositService) {
        this.amCodeDepositService = amCodeDepositService;
    }

    //region ------------------------------------------------------------------------------------------------ amCodeDeposit API

    /**
     *
     */
    @GetMapping(value = "/am-code-deposit", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<AmCodeDeposit>> listAmCodeDeposit(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<AmCodeDeposit> amCodeDepositList = amCodeDepositService.listAmCodeDeposit(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(amCodeDepositList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/am-code-deposit/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = amCodeDepositService.listAmCodeDepositCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/am-code-deposit/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeDeposit> getEmptyAmCodeDeposit() {
        AmCodeDeposit amCodeDeposit = new AmCodeDeposit();
        return new ResponseEntity<>(amCodeDeposit, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/am-code-deposit/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeDeposit> getAmCodeDeposit(@PathVariable("id") Long id) {
        AmCodeDeposit amCodeDeposit = amCodeDepositService.getAmCodeDeposit(id);
        return new ResponseEntity<>(amCodeDeposit, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/am-code-deposit")
    public ResponseEntity<Void> addAmCodeDeposit(@RequestBody AmCodeDeposit amCodeDeposit) {
        amCodeDepositService.saveAmCodeDeposit(amCodeDeposit);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/am-code-deposit/{id}")
    public ResponseEntity<Void> updateAmCodeDeposit(@PathVariable("id") Long id, @RequestBody AmCodeDeposit amCodeDeposit) throws Exception {
        if (!id.equals(amCodeDeposit.getId())) {
            return new ResponseEntity(HttpStatus.BAD_REQUEST);
        }
        amCodeDepositService.saveAmCodeDeposit(amCodeDeposit);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/am-code-deposit/{id}")
    public ResponseEntity<Void> removeAmCodeDeposit(@PathVariable("id") Long id) {
        amCodeDepositService.removeAmCodeDeposit(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
