/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.lease;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.lease.domain.LeaseContract;
import com.neofect.gts.core.services.lease.service.LeaseContractService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link LeaseContract}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class LeaseContractResource {

    private final LeaseContractService leaseContractService;

    public LeaseContractResource(LeaseContractService leaseContractService) {
        this.leaseContractService = leaseContractService;
    }

    //region ------------------------------------------------------------------------------------------------ leaseContract API

    /**
     *
     */
    @GetMapping(value = "/lease-contract", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<LeaseContract>> listLeaseContract(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<LeaseContract> leaseContractList = leaseContractService.listLeaseContract(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(leaseContractList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/lease-contract/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = leaseContractService.listLeaseContractCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/lease-contract/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<LeaseContract> getEmptyLeaseContract() {
        LeaseContract leaseContract = new LeaseContract();
        return new ResponseEntity<>(leaseContract, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/lease-contract/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<LeaseContract> getLeaseContract(@PathVariable("id") Long id) {
        LeaseContract leaseContract = leaseContractService.getLeaseContract(id);
        return new ResponseEntity<>(leaseContract, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/lease-contract")
    public ResponseEntity<Void> addLeaseContract(@RequestBody LeaseContract leaseContract) {
        leaseContractService.saveLeaseContract(leaseContract);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/lease-contract/{id}")
    public ResponseEntity<Void> updateLeaseContract(@PathVariable("id") Long id, @RequestBody LeaseContract leaseContract) throws Exception {
        // if (!id.equals(leaseContract.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        leaseContractService.saveLeaseContract(leaseContract);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/lease-contract/{id}")
    public ResponseEntity<Void> removeLeaseContract(@PathVariable("id") Long id) {
        leaseContractService.removeLeaseContract(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
