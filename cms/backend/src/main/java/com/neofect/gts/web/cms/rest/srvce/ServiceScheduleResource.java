/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.srvce;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.srvce.domain.ServiceSchedule;
import com.neofect.gts.core.services.srvce.service.ServiceScheduleService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link ServiceSchedule}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class ServiceScheduleResource {

    private final ServiceScheduleService serviceScheduleService;

    public ServiceScheduleResource(ServiceScheduleService serviceScheduleService) {
        this.serviceScheduleService = serviceScheduleService;
    }

    //region ------------------------------------------------------------------------------------------------ serviceSchedule API

    /**
     *
     */
    @GetMapping(value = "/service-schedule", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<ServiceSchedule>> listServiceSchedule(@RequestParam(value = "q", required = false) Map<String, Object> q)
            throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<ServiceSchedule> serviceScheduleList = serviceScheduleService.listServiceSchedule(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(serviceScheduleList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/service-schedule/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = serviceScheduleService.listServiceScheduleCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/service-schedule/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ServiceSchedule> getEmptyServiceSchedule() {
        ServiceSchedule serviceSchedule = new ServiceSchedule();
        return new ResponseEntity<>(serviceSchedule, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/service-schedule/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ServiceSchedule> getServiceSchedule(@PathVariable("id") Long id) {
        ServiceSchedule serviceSchedule = serviceScheduleService.getServiceSchedule(id);
        return new ResponseEntity<>(serviceSchedule, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/service-schedule")
    public ResponseEntity<Void> addServiceSchedule(@RequestBody ServiceSchedule serviceSchedule) {
        serviceScheduleService.saveServiceSchedule(serviceSchedule);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/service-schedule/{id}")
    public ResponseEntity<Void> updateServiceSchedule(@PathVariable("id") Long id, @RequestBody ServiceSchedule serviceSchedule) throws Exception {
        if (!id.equals(serviceSchedule.getId())) {
            return new ResponseEntity(HttpStatus.BAD_REQUEST);
        }
        serviceScheduleService.saveServiceSchedule(serviceSchedule);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/service-schedule/{id}")
    public ResponseEntity<Void> removeServiceSchedule(@PathVariable("id") Long id) {
        serviceScheduleService.removeServiceSchedule(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
