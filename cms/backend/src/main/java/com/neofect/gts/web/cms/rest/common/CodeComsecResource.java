/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.common;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.common.domain.CodeComsec;
import com.neofect.gts.core.services.common.service.CodeComsecService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link CodeComsec}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class CodeComsecResource {

    private final CodeComsecService codeComsecService;

    public CodeComsecResource(CodeComsecService codeComsecService) {
        this.codeComsecService = codeComsecService;
    }

    //region ------------------------------------------------------------------------------------------------ codeComsec API

    /**
     *
     */
    @GetMapping(value = "/code-comsec", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<CodeComsec>> listCodeComsec(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<CodeComsec> codeComsecList = codeComsecService.listCodeComsec(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(codeComsecList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/code-comsec/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = codeComsecService.listCodeComsecCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/code-comsec/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeComsec> getEmptyCodeComsec() {
        CodeComsec codeComsec = new CodeComsec();
        return new ResponseEntity<>(codeComsec, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/code-comsec/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeComsec> getCodeComsec(@PathVariable("id") Long id) {
        CodeComsec codeComsec = codeComsecService.getCodeComsec(id);
        return new ResponseEntity<>(codeComsec, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/code-comsec")
    public ResponseEntity<Void> addCodeComsec(@RequestBody CodeComsec codeComsec) {
        codeComsecService.saveCodeComsec(codeComsec);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/code-comsec/{id}")
    public ResponseEntity<Void> updateCodeComsec(@PathVariable("id") Long id, @RequestBody CodeComsec codeComsec) throws Exception {
        if (!id.equals(codeComsec.getId())) {
            return new ResponseEntity(HttpStatus.BAD_REQUEST);
        }
        codeComsecService.saveCodeComsec(codeComsec);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/code-comsec/{id}")
    public ResponseEntity<Void> removeCodeComsec(@PathVariable("id") Long id) {
        codeComsecService.removeCodeComsec(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
