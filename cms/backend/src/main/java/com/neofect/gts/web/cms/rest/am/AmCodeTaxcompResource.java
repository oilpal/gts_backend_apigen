/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.am;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.am.domain.AmCodeTaxcomp;
import com.neofect.gts.core.services.am.service.AmCodeTaxcompService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link AmCodeTaxcomp}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class AmCodeTaxcompResource {

    private final AmCodeTaxcompService amCodeTaxcompService;

    public AmCodeTaxcompResource(AmCodeTaxcompService amCodeTaxcompService) {
        this.amCodeTaxcompService = amCodeTaxcompService;
    }

    //region ------------------------------------------------------------------------------------------------ amCodeTaxcomp API

    /**
     *
     */
    @GetMapping(value = "/am-code-taxcomp", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<AmCodeTaxcomp>> listAmCodeTaxcomp(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<AmCodeTaxcomp> amCodeTaxcompList = amCodeTaxcompService.listAmCodeTaxcomp(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(amCodeTaxcompList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/am-code-taxcomp/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = amCodeTaxcompService.listAmCodeTaxcompCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/am-code-taxcomp/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeTaxcomp> getEmptyAmCodeTaxcomp() {
        AmCodeTaxcomp amCodeTaxcomp = new AmCodeTaxcomp();
        return new ResponseEntity<>(amCodeTaxcomp, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/am-code-taxcomp/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeTaxcomp> getAmCodeTaxcomp(@PathVariable("id") Long id) {
        AmCodeTaxcomp amCodeTaxcomp = amCodeTaxcompService.getAmCodeTaxcomp(id);
        return new ResponseEntity<>(amCodeTaxcomp, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/am-code-taxcomp")
    public ResponseEntity<Void> addAmCodeTaxcomp(@RequestBody AmCodeTaxcomp amCodeTaxcomp) {
        amCodeTaxcompService.saveAmCodeTaxcomp(amCodeTaxcomp);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/am-code-taxcomp/{id}")
    public ResponseEntity<Void> updateAmCodeTaxcomp(@PathVariable("id") Long id, @RequestBody AmCodeTaxcomp amCodeTaxcomp) throws Exception {
        // if (!id.equals(amCodeTaxcomp.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        amCodeTaxcompService.saveAmCodeTaxcomp(amCodeTaxcomp);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/am-code-taxcomp/{id}")
    public ResponseEntity<Void> removeAmCodeTaxcomp(@PathVariable("id") Long id) {
        amCodeTaxcompService.removeAmCodeTaxcomp(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
