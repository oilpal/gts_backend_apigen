/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.am;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.am.domain.AmCodeValidMast;
import com.neofect.gts.core.services.am.service.AmCodeValidMastService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link AmCodeValidMast}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class AmCodeValidMastResource {

    private final AmCodeValidMastService amCodeValidMastService;

    public AmCodeValidMastResource(AmCodeValidMastService amCodeValidMastService) {
        this.amCodeValidMastService = amCodeValidMastService;
    }

    //region ------------------------------------------------------------------------------------------------ amCodeValidMast API

    /**
     *
     */
    @GetMapping(value = "/am-code-valid-mast", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<AmCodeValidMast>> listAmCodeValidMast(@RequestParam(value = "q", required = false) Map<String, Object> q)
            throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<AmCodeValidMast> amCodeValidMastList = amCodeValidMastService.listAmCodeValidMast(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(amCodeValidMastList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/am-code-valid-mast/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = amCodeValidMastService.listAmCodeValidMastCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/am-code-valid-mast/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeValidMast> getEmptyAmCodeValidMast() {
        AmCodeValidMast amCodeValidMast = new AmCodeValidMast();
        return new ResponseEntity<>(amCodeValidMast, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/am-code-valid-mast/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeValidMast> getAmCodeValidMast(@PathVariable("id") Long id) {
        AmCodeValidMast amCodeValidMast = amCodeValidMastService.getAmCodeValidMast(id);
        return new ResponseEntity<>(amCodeValidMast, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/am-code-valid-mast")
    public ResponseEntity<Void> addAmCodeValidMast(@RequestBody AmCodeValidMast amCodeValidMast) {
        amCodeValidMastService.saveAmCodeValidMast(amCodeValidMast);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/am-code-valid-mast/{id}")
    public ResponseEntity<Void> updateAmCodeValidMast(@PathVariable("id") Long id, @RequestBody AmCodeValidMast amCodeValidMast) throws Exception {
        if (!id.equals(amCodeValidMast.getId())) {
            return new ResponseEntity(HttpStatus.BAD_REQUEST);
        }
        amCodeValidMastService.saveAmCodeValidMast(amCodeValidMast);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/am-code-valid-mast/{id}")
    public ResponseEntity<Void> removeAmCodeValidMast(@PathVariable("id") Long id) {
        amCodeValidMastService.removeAmCodeValidMast(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
