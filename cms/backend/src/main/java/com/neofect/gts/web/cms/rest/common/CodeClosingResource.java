/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.common;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.common.domain.CodeClosing;
import com.neofect.gts.core.services.common.service.CodeClosingService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link CodeClosing}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class CodeClosingResource {

    private final CodeClosingService codeClosingService;

    public CodeClosingResource(CodeClosingService codeClosingService) {
        this.codeClosingService = codeClosingService;
    }

    //region ------------------------------------------------------------------------------------------------ codeClosing API

    /**
     *
     */
    @GetMapping(value = "/code-closing", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<CodeClosing>> listCodeClosing(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<CodeClosing> codeClosingList = codeClosingService.listCodeClosing(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(codeClosingList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/code-closing/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = codeClosingService.listCodeClosingCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/code-closing/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeClosing> getEmptyCodeClosing() {
        CodeClosing codeClosing = new CodeClosing();
        return new ResponseEntity<>(codeClosing, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/code-closing/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeClosing> getCodeClosing(@PathVariable("id") Long id) {
        CodeClosing codeClosing = codeClosingService.getCodeClosing(id);
        return new ResponseEntity<>(codeClosing, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/code-closing")
    public ResponseEntity<Void> addCodeClosing(@RequestBody CodeClosing codeClosing) {
        codeClosingService.saveCodeClosing(codeClosing);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/code-closing/{id}")
    public ResponseEntity<Void> updateCodeClosing(@PathVariable("id") Long id, @RequestBody CodeClosing codeClosing) throws Exception {
        // if (!id.equals(codeClosing.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        codeClosingService.saveCodeClosing(codeClosing);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/code-closing/{id}")
    public ResponseEntity<Void> removeCodeClosing(@PathVariable("id") Long id) {
        codeClosingService.removeCodeClosing(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
