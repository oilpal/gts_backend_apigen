/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.am;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.am.domain.AmCalcList;
import com.neofect.gts.core.services.am.service.AmCalcListService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link AmCalcList}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class AmCalcListResource {

    private final AmCalcListService amCalcListService;

    public AmCalcListResource(AmCalcListService amCalcListService) {
        this.amCalcListService = amCalcListService;
    }

    //region ------------------------------------------------------------------------------------------------ amCalcList API

    /**
     *
     */
    @GetMapping(value = "/am-calc-list", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<AmCalcList>> listAmCalcList(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<AmCalcList> amCalcListList = amCalcListService.listAmCalcList(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(amCalcListList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/am-calc-list/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = amCalcListService.listAmCalcListCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/am-calc-list/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCalcList> getEmptyAmCalcList() {
        AmCalcList amCalcList = new AmCalcList();
        return new ResponseEntity<>(amCalcList, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/am-calc-list/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCalcList> getAmCalcList(@PathVariable("id") Long id) {
        AmCalcList amCalcList = amCalcListService.getAmCalcList(id);
        return new ResponseEntity<>(amCalcList, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/am-calc-list")
    public ResponseEntity<Void> addAmCalcList(@RequestBody AmCalcList amCalcList) {
        amCalcListService.saveAmCalcList(amCalcList);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/am-calc-list/{id}")
    public ResponseEntity<Void> updateAmCalcList(@PathVariable("id") Long id, @RequestBody AmCalcList amCalcList) throws Exception {
        // if (!id.equals(amCalcList.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        amCalcListService.saveAmCalcList(amCalcList);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/am-calc-list/{id}")
    public ResponseEntity<Void> removeAmCalcList(@PathVariable("id") Long id) {
        amCalcListService.removeAmCalcList(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
