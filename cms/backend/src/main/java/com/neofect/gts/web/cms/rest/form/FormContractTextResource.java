/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.form;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.form.domain.FormContractText;
import com.neofect.gts.core.services.form.service.FormContractTextService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link FormContractText}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class FormContractTextResource {

    private final FormContractTextService formContractTextService;

    public FormContractTextResource(FormContractTextService formContractTextService) {
        this.formContractTextService = formContractTextService;
    }

    //region ------------------------------------------------------------------------------------------------ formContractText API

    /**
     *
     */
    @GetMapping(value = "/form-contract-text", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<FormContractText>> listFormContractText(@RequestParam(value = "q", required = false) Map<String, Object> q)
            throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<FormContractText> formContractTextList = formContractTextService.listFormContractText(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(formContractTextList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/form-contract-text/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = formContractTextService.listFormContractTextCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/form-contract-text/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<FormContractText> getEmptyFormContractText() {
        FormContractText formContractText = new FormContractText();
        return new ResponseEntity<>(formContractText, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/form-contract-text/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<FormContractText> getFormContractText(@PathVariable("id") Long id) {
        FormContractText formContractText = formContractTextService.getFormContractText(id);
        return new ResponseEntity<>(formContractText, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/form-contract-text")
    public ResponseEntity<Void> addFormContractText(@RequestBody FormContractText formContractText) {
        formContractTextService.saveFormContractText(formContractText);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/form-contract-text/{id}")
    public ResponseEntity<Void> updateFormContractText(@PathVariable("id") Long id, @RequestBody FormContractText formContractText) throws Exception {
        // if (!id.equals(formContractText.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        formContractTextService.saveFormContractText(formContractText);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/form-contract-text/{id}")
    public ResponseEntity<Void> removeFormContractText(@PathVariable("id") Long id) {
        formContractTextService.removeFormContractText(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
