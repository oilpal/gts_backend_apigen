/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.sm;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.sm.domain.SumMm;
import com.neofect.gts.core.services.sm.service.SumMmService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link SumMm}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class SumMmResource {

    private final SumMmService sumMmService;

    public SumMmResource(SumMmService sumMmService) {
        this.sumMmService = sumMmService;
    }

    //region ------------------------------------------------------------------------------------------------ sumMm API

    /**
     *
     */
    @GetMapping(value = "/sum-mm", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<SumMm>> listSumMm(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<SumMm> sumMmList = sumMmService.listSumMm(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(sumMmList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/sum-mm/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = sumMmService.listSumMmCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/sum-mm/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SumMm> getEmptySumMm() {
        SumMm sumMm = new SumMm();
        return new ResponseEntity<>(sumMm, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/sum-mm/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SumMm> getSumMm(@PathVariable("id") Long id) {
        SumMm sumMm = sumMmService.getSumMm(id);
        return new ResponseEntity<>(sumMm, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/sum-mm")
    public ResponseEntity<Void> addSumMm(@RequestBody SumMm sumMm) {
        sumMmService.saveSumMm(sumMm);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/sum-mm/{id}")
    public ResponseEntity<Void> updateSumMm(@PathVariable("id") Long id, @RequestBody SumMm sumMm) throws Exception {
        // if (!id.equals(sumMm.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        sumMmService.saveSumMm(sumMm);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/sum-mm/{id}")
    public ResponseEntity<Void> removeSumMm(@PathVariable("id") Long id) {
        sumMmService.removeSumMm(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
