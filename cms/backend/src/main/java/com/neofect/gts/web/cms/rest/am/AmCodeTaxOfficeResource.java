/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.am;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.am.domain.AmCodeTaxOffice;
import com.neofect.gts.core.services.am.service.AmCodeTaxOfficeService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link AmCodeTaxOffice}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class AmCodeTaxOfficeResource {

    private final AmCodeTaxOfficeService amCodeTaxOfficeService;

    public AmCodeTaxOfficeResource(AmCodeTaxOfficeService amCodeTaxOfficeService) {
        this.amCodeTaxOfficeService = amCodeTaxOfficeService;
    }

    //region ------------------------------------------------------------------------------------------------ amCodeTaxOffice API

    /**
     *
     */
    @GetMapping(value = "/am-code-tax-office", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<AmCodeTaxOffice>> listAmCodeTaxOffice(@RequestParam(value = "q", required = false) Map<String, Object> q)
            throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<AmCodeTaxOffice> amCodeTaxOfficeList = amCodeTaxOfficeService.listAmCodeTaxOffice(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(amCodeTaxOfficeList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/am-code-tax-office/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = amCodeTaxOfficeService.listAmCodeTaxOfficeCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/am-code-tax-office/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeTaxOffice> getEmptyAmCodeTaxOffice() {
        AmCodeTaxOffice amCodeTaxOffice = new AmCodeTaxOffice();
        return new ResponseEntity<>(amCodeTaxOffice, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/am-code-tax-office/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCodeTaxOffice> getAmCodeTaxOffice(@PathVariable("id") Long id) {
        AmCodeTaxOffice amCodeTaxOffice = amCodeTaxOfficeService.getAmCodeTaxOffice(id);
        return new ResponseEntity<>(amCodeTaxOffice, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/am-code-tax-office")
    public ResponseEntity<Void> addAmCodeTaxOffice(@RequestBody AmCodeTaxOffice amCodeTaxOffice) {
        amCodeTaxOfficeService.saveAmCodeTaxOffice(amCodeTaxOffice);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/am-code-tax-office/{id}")
    public ResponseEntity<Void> updateAmCodeTaxOffice(@PathVariable("id") Long id, @RequestBody AmCodeTaxOffice amCodeTaxOffice) throws Exception {
        if (!id.equals(amCodeTaxOffice.getId())) {
            return new ResponseEntity(HttpStatus.BAD_REQUEST);
        }
        amCodeTaxOfficeService.saveAmCodeTaxOffice(amCodeTaxOffice);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/am-code-tax-office/{id}")
    public ResponseEntity<Void> removeAmCodeTaxOffice(@PathVariable("id") Long id) {
        amCodeTaxOfficeService.removeAmCodeTaxOffice(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
