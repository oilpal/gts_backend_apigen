/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.sm;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.sm.domain.SmAuthGroup;
import com.neofect.gts.core.services.sm.service.SmAuthGroupService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link SmAuthGroup}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class SmAuthGroupResource {

    private final SmAuthGroupService smAuthGroupService;

    public SmAuthGroupResource(SmAuthGroupService smAuthGroupService) {
        this.smAuthGroupService = smAuthGroupService;
    }

    //region ------------------------------------------------------------------------------------------------ smAuthGroup API

    /**
     *
     */
    @GetMapping(value = "/sm-auth-group", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<SmAuthGroup>> listSmAuthGroup(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<SmAuthGroup> smAuthGroupList = smAuthGroupService.listSmAuthGroup(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(smAuthGroupList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/sm-auth-group/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = smAuthGroupService.listSmAuthGroupCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/sm-auth-group/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmAuthGroup> getEmptySmAuthGroup() {
        SmAuthGroup smAuthGroup = new SmAuthGroup();
        return new ResponseEntity<>(smAuthGroup, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/sm-auth-group/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmAuthGroup> getSmAuthGroup(@PathVariable("id") Long id) {
        SmAuthGroup smAuthGroup = smAuthGroupService.getSmAuthGroup(id);
        return new ResponseEntity<>(smAuthGroup, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/sm-auth-group")
    public ResponseEntity<Void> addSmAuthGroup(@RequestBody SmAuthGroup smAuthGroup) {
        smAuthGroupService.saveSmAuthGroup(smAuthGroup);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/sm-auth-group/{id}")
    public ResponseEntity<Void> updateSmAuthGroup(@PathVariable("id") Long id, @RequestBody SmAuthGroup smAuthGroup) throws Exception {
        // if (!id.equals(smAuthGroup.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        smAuthGroupService.saveSmAuthGroup(smAuthGroup);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/sm-auth-group/{id}")
    public ResponseEntity<Void> removeSmAuthGroup(@PathVariable("id") Long id) {
        smAuthGroupService.removeSmAuthGroup(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
