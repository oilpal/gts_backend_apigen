/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.common;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.common.domain.CodeCust;
import com.neofect.gts.core.services.common.service.CodeCustService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link CodeCust}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class CodeCustResource {

    private final CodeCustService codeCustService;

    public CodeCustResource(CodeCustService codeCustService) {
        this.codeCustService = codeCustService;
    }

    //region ------------------------------------------------------------------------------------------------ codeCust API

    /**
     *
     */
    @GetMapping(value = "/code-cust", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<CodeCust>> listCodeCust(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<CodeCust> codeCustList = codeCustService.listCodeCust(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(codeCustList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/code-cust/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = codeCustService.listCodeCustCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/code-cust/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeCust> getEmptyCodeCust() {
        CodeCust codeCust = new CodeCust();
        return new ResponseEntity<>(codeCust, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/code-cust/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeCust> getCodeCust(@PathVariable("id") Long id) {
        CodeCust codeCust = codeCustService.getCodeCust(id);
        return new ResponseEntity<>(codeCust, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/code-cust")
    public ResponseEntity<Void> addCodeCust(@RequestBody CodeCust codeCust) {
        codeCustService.saveCodeCust(codeCust);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/code-cust/{id}")
    public ResponseEntity<Void> updateCodeCust(@PathVariable("id") Long id, @RequestBody CodeCust codeCust) throws Exception {
        if (!id.equals(codeCust.getId())) {
            return new ResponseEntity(HttpStatus.BAD_REQUEST);
        }
        codeCustService.saveCodeCust(codeCust);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/code-cust/{id}")
    public ResponseEntity<Void> removeCodeCust(@PathVariable("id") Long id) {
        codeCustService.removeCodeCust(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
