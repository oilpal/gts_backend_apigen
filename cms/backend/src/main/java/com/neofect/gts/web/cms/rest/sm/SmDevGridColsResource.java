/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.sm;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.sm.domain.SmDevGridCols;
import com.neofect.gts.core.services.sm.service.SmDevGridColsService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link SmDevGridCols}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class SmDevGridColsResource {

    private final SmDevGridColsService smDevGridColsService;

    public SmDevGridColsResource(SmDevGridColsService smDevGridColsService) {
        this.smDevGridColsService = smDevGridColsService;
    }

    //region ------------------------------------------------------------------------------------------------ smDevGridCols API

    /**
     *
     */
    @GetMapping(value = "/sm-dev-grid-cols", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<SmDevGridCols>> listSmDevGridCols(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<SmDevGridCols> smDevGridColsList = smDevGridColsService.listSmDevGridCols(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(smDevGridColsList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/sm-dev-grid-cols/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = smDevGridColsService.listSmDevGridColsCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/sm-dev-grid-cols/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmDevGridCols> getEmptySmDevGridCols() {
        SmDevGridCols smDevGridCols = new SmDevGridCols();
        return new ResponseEntity<>(smDevGridCols, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/sm-dev-grid-cols/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmDevGridCols> getSmDevGridCols(@PathVariable("id") Long id) {
        SmDevGridCols smDevGridCols = smDevGridColsService.getSmDevGridCols(id);
        return new ResponseEntity<>(smDevGridCols, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/sm-dev-grid-cols")
    public ResponseEntity<Void> addSmDevGridCols(@RequestBody SmDevGridCols smDevGridCols) {
        smDevGridColsService.saveSmDevGridCols(smDevGridCols);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/sm-dev-grid-cols/{id}")
    public ResponseEntity<Void> updateSmDevGridCols(@PathVariable("id") Long id, @RequestBody SmDevGridCols smDevGridCols) throws Exception {
        // if (!id.equals(smDevGridCols.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        smDevGridColsService.saveSmDevGridCols(smDevGridCols);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/sm-dev-grid-cols/{id}")
    public ResponseEntity<Void> removeSmDevGridCols(@PathVariable("id") Long id) {
        smDevGridColsService.removeSmDevGridCols(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
