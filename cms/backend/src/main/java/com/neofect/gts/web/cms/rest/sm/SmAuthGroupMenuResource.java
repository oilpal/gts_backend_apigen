/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.sm;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.sm.domain.SmAuthGroupMenu;
import com.neofect.gts.core.services.sm.service.SmAuthGroupMenuService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link SmAuthGroupMenu}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class SmAuthGroupMenuResource {

    private final SmAuthGroupMenuService smAuthGroupMenuService;

    public SmAuthGroupMenuResource(SmAuthGroupMenuService smAuthGroupMenuService) {
        this.smAuthGroupMenuService = smAuthGroupMenuService;
    }

    //region ------------------------------------------------------------------------------------------------ smAuthGroupMenu API

    /**
     *
     */
    @GetMapping(value = "/sm-auth-group-menu", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<SmAuthGroupMenu>> listSmAuthGroupMenu(@RequestParam(value = "q", required = false) Map<String, Object> q)
            throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<SmAuthGroupMenu> smAuthGroupMenuList = smAuthGroupMenuService.listSmAuthGroupMenu(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(smAuthGroupMenuList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/sm-auth-group-menu/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = smAuthGroupMenuService.listSmAuthGroupMenuCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/sm-auth-group-menu/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmAuthGroupMenu> getEmptySmAuthGroupMenu() {
        SmAuthGroupMenu smAuthGroupMenu = new SmAuthGroupMenu();
        return new ResponseEntity<>(smAuthGroupMenu, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/sm-auth-group-menu/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmAuthGroupMenu> getSmAuthGroupMenu(@PathVariable("id") Long id) {
        SmAuthGroupMenu smAuthGroupMenu = smAuthGroupMenuService.getSmAuthGroupMenu(id);
        return new ResponseEntity<>(smAuthGroupMenu, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/sm-auth-group-menu")
    public ResponseEntity<Void> addSmAuthGroupMenu(@RequestBody SmAuthGroupMenu smAuthGroupMenu) {
        smAuthGroupMenuService.saveSmAuthGroupMenu(smAuthGroupMenu);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/sm-auth-group-menu/{id}")
    public ResponseEntity<Void> updateSmAuthGroupMenu(@PathVariable("id") Long id, @RequestBody SmAuthGroupMenu smAuthGroupMenu) throws Exception {
        // if (!id.equals(smAuthGroupMenu.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        smAuthGroupMenuService.saveSmAuthGroupMenu(smAuthGroupMenu);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/sm-auth-group-menu/{id}")
    public ResponseEntity<Void> removeSmAuthGroupMenu(@PathVariable("id") Long id) {
        smAuthGroupMenuService.removeSmAuthGroupMenu(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
