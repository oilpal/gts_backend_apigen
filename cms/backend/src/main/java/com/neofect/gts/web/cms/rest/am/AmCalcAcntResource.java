/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.am;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.am.domain.AmCalcAcnt;
import com.neofect.gts.core.services.am.service.AmCalcAcntService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link AmCalcAcnt}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class AmCalcAcntResource {

    private final AmCalcAcntService amCalcAcntService;

    public AmCalcAcntResource(AmCalcAcntService amCalcAcntService) {
        this.amCalcAcntService = amCalcAcntService;
    }

    //region ------------------------------------------------------------------------------------------------ amCalcAcnt API

    /**
     *
     */
    @GetMapping(value = "/am-calc-acnt", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<AmCalcAcnt>> listAmCalcAcnt(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<AmCalcAcnt> amCalcAcntList = amCalcAcntService.listAmCalcAcnt(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(amCalcAcntList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/am-calc-acnt/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = amCalcAcntService.listAmCalcAcntCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/am-calc-acnt/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCalcAcnt> getEmptyAmCalcAcnt() {
        AmCalcAcnt amCalcAcnt = new AmCalcAcnt();
        return new ResponseEntity<>(amCalcAcnt, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/am-calc-acnt/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<AmCalcAcnt> getAmCalcAcnt(@PathVariable("id") Long id) {
        AmCalcAcnt amCalcAcnt = amCalcAcntService.getAmCalcAcnt(id);
        return new ResponseEntity<>(amCalcAcnt, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/am-calc-acnt")
    public ResponseEntity<Void> addAmCalcAcnt(@RequestBody AmCalcAcnt amCalcAcnt) {
        amCalcAcntService.saveAmCalcAcnt(amCalcAcnt);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/am-calc-acnt/{id}")
    public ResponseEntity<Void> updateAmCalcAcnt(@PathVariable("id") Long id, @RequestBody AmCalcAcnt amCalcAcnt) throws Exception {
        // if (!id.equals(amCalcAcnt.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        amCalcAcntService.saveAmCalcAcnt(amCalcAcnt);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/am-calc-acnt/{id}")
    public ResponseEntity<Void> removeAmCalcAcnt(@PathVariable("id") Long id) {
        amCalcAcntService.removeAmCalcAcnt(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
