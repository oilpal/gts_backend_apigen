/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.sm;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.sm.domain.SmAuthUser;
import com.neofect.gts.core.services.sm.service.SmAuthUserService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link SmAuthUser}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class SmAuthUserResource {

    private final SmAuthUserService smAuthUserService;

    public SmAuthUserResource(SmAuthUserService smAuthUserService) {
        this.smAuthUserService = smAuthUserService;
    }

    //region ------------------------------------------------------------------------------------------------ smAuthUser API

    /**
     *
     */
    @GetMapping(value = "/sm-auth-user", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<SmAuthUser>> listSmAuthUser(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<SmAuthUser> smAuthUserList = smAuthUserService.listSmAuthUser(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(smAuthUserList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/sm-auth-user/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = smAuthUserService.listSmAuthUserCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/sm-auth-user/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmAuthUser> getEmptySmAuthUser() {
        SmAuthUser smAuthUser = new SmAuthUser();
        return new ResponseEntity<>(smAuthUser, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/sm-auth-user/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmAuthUser> getSmAuthUser(@PathVariable("id") Long id) {
        SmAuthUser smAuthUser = smAuthUserService.getSmAuthUser(id);
        return new ResponseEntity<>(smAuthUser, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/sm-auth-user")
    public ResponseEntity<Void> addSmAuthUser(@RequestBody SmAuthUser smAuthUser) {
        smAuthUserService.saveSmAuthUser(smAuthUser);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/sm-auth-user/{id}")
    public ResponseEntity<Void> updateSmAuthUser(@PathVariable("id") Long id, @RequestBody SmAuthUser smAuthUser) throws Exception {
        // if (!id.equals(smAuthUser.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        smAuthUserService.saveSmAuthUser(smAuthUser);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/sm-auth-user/{id}")
    public ResponseEntity<Void> removeSmAuthUser(@PathVariable("id") Long id) {
        smAuthUserService.removeSmAuthUser(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
