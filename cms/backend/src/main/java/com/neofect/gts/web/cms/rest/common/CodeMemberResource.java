/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.common;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.common.domain.CodeMember;
import com.neofect.gts.core.services.common.service.CodeMemberService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link CodeMember}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class CodeMemberResource {

    private final CodeMemberService codeMemberService;

    public CodeMemberResource(CodeMemberService codeMemberService) {
        this.codeMemberService = codeMemberService;
    }

    //region ------------------------------------------------------------------------------------------------ codeMember API

    /**
     *
     */
    @GetMapping(value = "/code-member", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<CodeMember>> listCodeMember(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<CodeMember> codeMemberList = codeMemberService.listCodeMember(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(codeMemberList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/code-member/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = codeMemberService.listCodeMemberCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/code-member/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeMember> getEmptyCodeMember() {
        CodeMember codeMember = new CodeMember();
        return new ResponseEntity<>(codeMember, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/code-member/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeMember> getCodeMember(@PathVariable("id") Long id) {
        CodeMember codeMember = codeMemberService.getCodeMember(id);
        return new ResponseEntity<>(codeMember, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/code-member")
    public ResponseEntity<Void> addCodeMember(@RequestBody CodeMember codeMember) {
        codeMemberService.saveCodeMember(codeMember);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/code-member/{id}")
    public ResponseEntity<Void> updateCodeMember(@PathVariable("id") Long id, @RequestBody CodeMember codeMember) throws Exception {
        if (!id.equals(codeMember.getId())) {
            return new ResponseEntity(HttpStatus.BAD_REQUEST);
        }
        codeMemberService.saveCodeMember(codeMember);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/code-member/{id}")
    public ResponseEntity<Void> removeCodeMember(@PathVariable("id") Long id) {
        codeMemberService.removeCodeMember(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
