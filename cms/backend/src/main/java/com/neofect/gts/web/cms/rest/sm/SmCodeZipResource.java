/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.sm;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.sm.domain.SmCodeZip;
import com.neofect.gts.core.services.sm.service.SmCodeZipService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link SmCodeZip}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class SmCodeZipResource {

    private final SmCodeZipService smCodeZipService;

    public SmCodeZipResource(SmCodeZipService smCodeZipService) {
        this.smCodeZipService = smCodeZipService;
    }

    //region ------------------------------------------------------------------------------------------------ smCodeZip API

    /**
     *
     */
    @GetMapping(value = "/sm-code-zip", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<SmCodeZip>> listSmCodeZip(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<SmCodeZip> smCodeZipList = smCodeZipService.listSmCodeZip(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(smCodeZipList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/sm-code-zip/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = smCodeZipService.listSmCodeZipCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/sm-code-zip/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmCodeZip> getEmptySmCodeZip() {
        SmCodeZip smCodeZip = new SmCodeZip();
        return new ResponseEntity<>(smCodeZip, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/sm-code-zip/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmCodeZip> getSmCodeZip(@PathVariable("id") Long id) {
        SmCodeZip smCodeZip = smCodeZipService.getSmCodeZip(id);
        return new ResponseEntity<>(smCodeZip, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/sm-code-zip")
    public ResponseEntity<Void> addSmCodeZip(@RequestBody SmCodeZip smCodeZip) {
        smCodeZipService.saveSmCodeZip(smCodeZip);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/sm-code-zip/{id}")
    public ResponseEntity<Void> updateSmCodeZip(@PathVariable("id") Long id, @RequestBody SmCodeZip smCodeZip) throws Exception {
        // if (!id.equals(smCodeZip.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        smCodeZipService.saveSmCodeZip(smCodeZip);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/sm-code-zip/{id}")
    public ResponseEntity<Void> removeSmCodeZip(@PathVariable("id") Long id) {
        smCodeZipService.removeSmCodeZip(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
