/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.sm;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.sm.domain.SmCommFile;
import com.neofect.gts.core.services.sm.service.SmCommFileService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link SmCommFile}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class SmCommFileResource {

    private final SmCommFileService smCommFileService;

    public SmCommFileResource(SmCommFileService smCommFileService) {
        this.smCommFileService = smCommFileService;
    }

    //region ------------------------------------------------------------------------------------------------ smCommFile API

    /**
     *
     */
    @GetMapping(value = "/sm-comm-file", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<SmCommFile>> listSmCommFile(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<SmCommFile> smCommFileList = smCommFileService.listSmCommFile(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(smCommFileList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/sm-comm-file/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = smCommFileService.listSmCommFileCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/sm-comm-file/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmCommFile> getEmptySmCommFile() {
        SmCommFile smCommFile = new SmCommFile();
        return new ResponseEntity<>(smCommFile, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/sm-comm-file/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SmCommFile> getSmCommFile(@PathVariable("id") Long id) {
        SmCommFile smCommFile = smCommFileService.getSmCommFile(id);
        return new ResponseEntity<>(smCommFile, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/sm-comm-file")
    public ResponseEntity<Void> addSmCommFile(@RequestBody SmCommFile smCommFile) {
        smCommFileService.saveSmCommFile(smCommFile);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/sm-comm-file/{id}")
    public ResponseEntity<Void> updateSmCommFile(@PathVariable("id") Long id, @RequestBody SmCommFile smCommFile) throws Exception {
        if (!id.equals(smCommFile.getId())) {
            return new ResponseEntity(HttpStatus.BAD_REQUEST);
        }
        smCommFileService.saveSmCommFile(smCommFile);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/sm-comm-file/{id}")
    public ResponseEntity<Void> removeSmCommFile(@PathVariable("id") Long id) {
        smCommFileService.removeSmCommFile(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
