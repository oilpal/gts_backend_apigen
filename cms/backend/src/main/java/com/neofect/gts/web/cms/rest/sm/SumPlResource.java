/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.sm;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.sm.domain.SumPl;
import com.neofect.gts.core.services.sm.service.SumPlService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link SumPl}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class SumPlResource {

    private final SumPlService sumPlService;

    public SumPlResource(SumPlService sumPlService) {
        this.sumPlService = sumPlService;
    }

    //region ------------------------------------------------------------------------------------------------ sumPl API

    /**
     *
     */
    @GetMapping(value = "/sum-pl", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<SumPl>> listSumPl(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<SumPl> sumPlList = sumPlService.listSumPl(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(sumPlList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/sum-pl/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = sumPlService.listSumPlCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/sum-pl/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SumPl> getEmptySumPl() {
        SumPl sumPl = new SumPl();
        return new ResponseEntity<>(sumPl, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/sum-pl/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SumPl> getSumPl(@PathVariable("id") Long id) {
        SumPl sumPl = sumPlService.getSumPl(id);
        return new ResponseEntity<>(sumPl, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/sum-pl")
    public ResponseEntity<Void> addSumPl(@RequestBody SumPl sumPl) {
        sumPlService.saveSumPl(sumPl);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/sum-pl/{id}")
    public ResponseEntity<Void> updateSumPl(@PathVariable("id") Long id, @RequestBody SumPl sumPl) throws Exception {
        // if (!id.equals(sumPl.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        sumPlService.saveSumPl(sumPl);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/sum-pl/{id}")
    public ResponseEntity<Void> removeSumPl(@PathVariable("id") Long id) {
        sumPlService.removeSumPl(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
