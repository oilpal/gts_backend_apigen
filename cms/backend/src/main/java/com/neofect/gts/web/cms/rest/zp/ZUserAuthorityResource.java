/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.zp;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.zp.domain.ZUserAuthority;
import com.neofect.gts.core.services.zp.service.ZUserAuthorityService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link ZUserAuthority}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class ZUserAuthorityResource {

    private final ZUserAuthorityService zUserAuthorityService;

    public ZUserAuthorityResource(ZUserAuthorityService zUserAuthorityService) {
        this.zUserAuthorityService = zUserAuthorityService;
    }

    //region ------------------------------------------------------------------------------------------------ zUserAuthority API

    /**
     *
     */
    @GetMapping(value = "/z-user-authority", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<ZUserAuthority>> listZUserAuthority(@RequestParam(value = "q", required = false) Map<String, Object> q)
            throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<ZUserAuthority> zUserAuthorityList = zUserAuthorityService.listZUserAuthority(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(zUserAuthorityList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/z-user-authority/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = zUserAuthorityService.listZUserAuthorityCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/z-user-authority/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ZUserAuthority> getEmptyZUserAuthority() {
        ZUserAuthority zUserAuthority = new ZUserAuthority();
        return new ResponseEntity<>(zUserAuthority, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/z-user-authority/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ZUserAuthority> getZUserAuthority(@PathVariable("id") Long id) {
        ZUserAuthority zUserAuthority = zUserAuthorityService.getZUserAuthority(id);
        return new ResponseEntity<>(zUserAuthority, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/z-user-authority")
    public ResponseEntity<Void> addZUserAuthority(@RequestBody ZUserAuthority zUserAuthority) {
        zUserAuthorityService.saveZUserAuthority(zUserAuthority);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/z-user-authority/{id}")
    public ResponseEntity<Void> updateZUserAuthority(@PathVariable("id") Long id, @RequestBody ZUserAuthority zUserAuthority) throws Exception {
        // if (!id.equals(zUserAuthority.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        zUserAuthorityService.saveZUserAuthority(zUserAuthority);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/z-user-authority/{id}")
    public ResponseEntity<Void> removeZUserAuthority(@PathVariable("id") Long id) {
        zUserAuthorityService.removeZUserAuthority(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
