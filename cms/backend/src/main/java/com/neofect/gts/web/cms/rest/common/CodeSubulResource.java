/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.common;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.common.domain.CodeSubul;
import com.neofect.gts.core.services.common.service.CodeSubulService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link CodeSubul}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class CodeSubulResource {

    private final CodeSubulService codeSubulService;

    public CodeSubulResource(CodeSubulService codeSubulService) {
        this.codeSubulService = codeSubulService;
    }

    //region ------------------------------------------------------------------------------------------------ codeSubul API

    /**
     *
     */
    @GetMapping(value = "/code-subul", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<CodeSubul>> listCodeSubul(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<CodeSubul> codeSubulList = codeSubulService.listCodeSubul(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(codeSubulList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/code-subul/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = codeSubulService.listCodeSubulCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/code-subul/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeSubul> getEmptyCodeSubul() {
        CodeSubul codeSubul = new CodeSubul();
        return new ResponseEntity<>(codeSubul, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/code-subul/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeSubul> getCodeSubul(@PathVariable("id") Long id) {
        CodeSubul codeSubul = codeSubulService.getCodeSubul(id);
        return new ResponseEntity<>(codeSubul, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/code-subul")
    public ResponseEntity<Void> addCodeSubul(@RequestBody CodeSubul codeSubul) {
        codeSubulService.saveCodeSubul(codeSubul);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/code-subul/{id}")
    public ResponseEntity<Void> updateCodeSubul(@PathVariable("id") Long id, @RequestBody CodeSubul codeSubul) throws Exception {
        if (!id.equals(codeSubul.getId())) {
            return new ResponseEntity(HttpStatus.BAD_REQUEST);
        }
        codeSubulService.saveCodeSubul(codeSubul);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/code-subul/{id}")
    public ResponseEntity<Void> removeCodeSubul(@PathVariable("id") Long id) {
        codeSubulService.removeCodeSubul(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
