/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.srvce;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.srvce.domain.ServiceSummary;
import com.neofect.gts.core.services.srvce.service.ServiceSummaryService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link ServiceSummary}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class ServiceSummaryResource {

    private final ServiceSummaryService serviceSummaryService;

    public ServiceSummaryResource(ServiceSummaryService serviceSummaryService) {
        this.serviceSummaryService = serviceSummaryService;
    }

    //region ------------------------------------------------------------------------------------------------ serviceSummary API

    /**
     *
     */
    @GetMapping(value = "/service-summary", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<ServiceSummary>> listServiceSummary(@RequestParam(value = "q", required = false) Map<String, Object> q)
            throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<ServiceSummary> serviceSummaryList = serviceSummaryService.listServiceSummary(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(serviceSummaryList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/service-summary/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = serviceSummaryService.listServiceSummaryCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/service-summary/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ServiceSummary> getEmptyServiceSummary() {
        ServiceSummary serviceSummary = new ServiceSummary();
        return new ResponseEntity<>(serviceSummary, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/service-summary/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ServiceSummary> getServiceSummary(@PathVariable("id") Long id) {
        ServiceSummary serviceSummary = serviceSummaryService.getServiceSummary(id);
        return new ResponseEntity<>(serviceSummary, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/service-summary")
    public ResponseEntity<Void> addServiceSummary(@RequestBody ServiceSummary serviceSummary) {
        serviceSummaryService.saveServiceSummary(serviceSummary);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/service-summary/{id}")
    public ResponseEntity<Void> updateServiceSummary(@PathVariable("id") Long id, @RequestBody ServiceSummary serviceSummary) throws Exception {
        // if (!id.equals(serviceSummary.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        serviceSummaryService.saveServiceSummary(serviceSummary);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/service-summary/{id}")
    public ResponseEntity<Void> removeServiceSummary(@PathVariable("id") Long id) {
        serviceSummaryService.removeServiceSummary(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
