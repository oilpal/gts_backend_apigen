/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.common;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.common.domain.CodeDept;
import com.neofect.gts.core.services.common.service.CodeDeptService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link CodeDept}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class CodeDeptResource {

    private final CodeDeptService codeDeptService;

    public CodeDeptResource(CodeDeptService codeDeptService) {
        this.codeDeptService = codeDeptService;
    }

    //region ------------------------------------------------------------------------------------------------ codeDept API

    /**
     *
     */
    @GetMapping(value = "/code-dept", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<CodeDept>> listCodeDept(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<CodeDept> codeDeptList = codeDeptService.listCodeDept(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(codeDeptList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/code-dept/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = codeDeptService.listCodeDeptCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/code-dept/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeDept> getEmptyCodeDept() {
        CodeDept codeDept = new CodeDept();
        return new ResponseEntity<>(codeDept, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/code-dept/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeDept> getCodeDept(@PathVariable("id") Long id) {
        CodeDept codeDept = codeDeptService.getCodeDept(id);
        return new ResponseEntity<>(codeDept, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/code-dept")
    public ResponseEntity<Void> addCodeDept(@RequestBody CodeDept codeDept) {
        codeDeptService.saveCodeDept(codeDept);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/code-dept/{id}")
    public ResponseEntity<Void> updateCodeDept(@PathVariable("id") Long id, @RequestBody CodeDept codeDept) throws Exception {
        if (!id.equals(codeDept.getId())) {
            return new ResponseEntity(HttpStatus.BAD_REQUEST);
        }
        codeDeptService.saveCodeDept(codeDept);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/code-dept/{id}")
    public ResponseEntity<Void> removeCodeDept(@PathVariable("id") Long id) {
        codeDeptService.removeCodeDept(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
