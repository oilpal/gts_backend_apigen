/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.common;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.common.domain.CodeDepositno;
import com.neofect.gts.core.services.common.service.CodeDepositnoService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link CodeDepositno}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class CodeDepositnoResource {

    private final CodeDepositnoService codeDepositnoService;

    public CodeDepositnoResource(CodeDepositnoService codeDepositnoService) {
        this.codeDepositnoService = codeDepositnoService;
    }

    //region ------------------------------------------------------------------------------------------------ codeDepositno API

    /**
     *
     */
    @GetMapping(value = "/code-depositno", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<CodeDepositno>> listCodeDepositno(@RequestParam(value = "q", required = false) Map<String, Object> q) throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<CodeDepositno> codeDepositnoList = codeDepositnoService.listCodeDepositno(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(codeDepositnoList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/code-depositno/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = codeDepositnoService.listCodeDepositnoCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/code-depositno/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeDepositno> getEmptyCodeDepositno() {
        CodeDepositno codeDepositno = new CodeDepositno();
        return new ResponseEntity<>(codeDepositno, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/code-depositno/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<CodeDepositno> getCodeDepositno(@PathVariable("id") Long id) {
        CodeDepositno codeDepositno = codeDepositnoService.getCodeDepositno(id);
        return new ResponseEntity<>(codeDepositno, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/code-depositno")
    public ResponseEntity<Void> addCodeDepositno(@RequestBody CodeDepositno codeDepositno) {
        codeDepositnoService.saveCodeDepositno(codeDepositno);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/code-depositno/{id}")
    public ResponseEntity<Void> updateCodeDepositno(@PathVariable("id") Long id, @RequestBody CodeDepositno codeDepositno) throws Exception {
        // if (!id.equals(codeDepositno.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        codeDepositnoService.saveCodeDepositno(codeDepositno);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/code-depositno/{id}")
    public ResponseEntity<Void> removeCodeDepositno(@PathVariable("id") Long id) {
        codeDepositnoService.removeCodeDepositno(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
