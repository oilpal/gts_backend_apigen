/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/rest/Rest.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.web.cms.rest.slip;

import java.net.URISyntaxException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import com.google.common.collect.ImmutableMap;
import com.neofect.gts.core.services.slip.domain.SlipSaleDetail;
import com.neofect.gts.core.services.slip.service.SlipSaleDetailService;
import com.unvus.web.util.PaginationUtil;

import lombok.extern.slf4j.Slf4j;

/**
 * Rest for {@link SlipSaleDetail}<br/>
 */
@Slf4j
@RestController
@RequestMapping("/api")
public class SlipSaleDetailResource {

    private final SlipSaleDetailService slipSaleDetailService;

    public SlipSaleDetailResource(SlipSaleDetailService slipSaleDetailService) {
        this.slipSaleDetailService = slipSaleDetailService;
    }

    //region ------------------------------------------------------------------------------------------------ slipSaleDetail API

    /**
     *
     */
    @GetMapping(value = "/slip-sale-detail", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<List<SlipSaleDetail>> listSlipSaleDetail(@RequestParam(value = "q", required = false) Map<String, Object> q)
            throws URISyntaxException {
        if (q == null) {
            q = new HashMap();
        }

        List<SlipSaleDetail> slipSaleDetailList = slipSaleDetailService.listSlipSaleDetail(q);

        HttpHeaders headers = PaginationUtil.generatePaginationHttpHeaders();

        return new ResponseEntity<>(slipSaleDetailList, headers, HttpStatus.OK);
    }

    /**
     * get count
     * @return
     */
    @GetMapping(value = "/slip-sale-detail/_count", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<Map> getCount(@RequestParam(value = "q", required = false) Map<String, Object> q) {
        int count = slipSaleDetailService.listSlipSaleDetailCnt(q);

        return new ResponseEntity<>(ImmutableMap.of("count", count), HttpStatus.OK);
    }

    /**
     * get empty object
     * @return
     */
    @GetMapping(value = "/slip-sale-detail/_empty", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SlipSaleDetail> getEmptySlipSaleDetail() {
        SlipSaleDetail slipSaleDetail = new SlipSaleDetail();
        return new ResponseEntity<>(slipSaleDetail, HttpStatus.OK);
    }

    /**
     * get
     */
    @GetMapping(value = "/slip-sale-detail/{id}", produces = MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<SlipSaleDetail> getSlipSaleDetail(@PathVariable("id") Long id) {
        SlipSaleDetail slipSaleDetail = slipSaleDetailService.getSlipSaleDetail(id);
        return new ResponseEntity<>(slipSaleDetail, HttpStatus.OK);
    }

    /**
     * add
     */
    @PostMapping("/slip-sale-detail")
    public ResponseEntity<Void> addSlipSaleDetail(@RequestBody SlipSaleDetail slipSaleDetail) {
        slipSaleDetailService.saveSlipSaleDetail(slipSaleDetail);

        return ResponseEntity.ok().build();
    }

    /**
     * modify
     */
    @PutMapping("/slip-sale-detail/{id}")
    public ResponseEntity<Void> updateSlipSaleDetail(@PathVariable("id") Long id, @RequestBody SlipSaleDetail slipSaleDetail) throws Exception {
        // if (!id.equals(slipSaleDetail.getId())) {
        //     return new ResponseEntity(HttpStatus.BAD_REQUEST);
        // }
        slipSaleDetailService.saveSlipSaleDetail(slipSaleDetail);
        return ResponseEntity.ok().build();
    }

    /**
     * delete
     */
    @DeleteMapping("/slip-sale-detail/{id}")
    public ResponseEntity<Void> removeSlipSaleDetail(@PathVariable("id") Long id) {
        slipSaleDetailService.removeSlipSaleDetail(id);
        return ResponseEntity.ok().build();
    }

    //endregion

}
