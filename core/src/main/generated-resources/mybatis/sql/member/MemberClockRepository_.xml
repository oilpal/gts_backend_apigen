<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/MybatisBase.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.member.repository.MemberClockRepository_">

    <resultMap id="simpleMemberClockResult_" type="com.neofect.gts.core.services.member.domain.MemberClock">
        <!-- 사번 -->
        <id     property="memberId" column="MEMBER_ID"/>
        <!-- 출결일자 -->
        <id     property="clockDate" column="CLOCK_DATE"/>
        <!-- 출결구분코드 -->
        <result property="clockDiv" column="CLOCK_DIV"/>
        <!-- 출결여부 -->
        <result property="clockYn" column="CLOCK_YN"/>
        <!-- 비고 -->
        <result property="remark" column="REMARK"/>
        
        <result property="ip" column="IP"/>
        
        <result property="rowInputEmpNo" column="ROW_INPUT_EMP_NO"/>
        
        <result property="rowInputDate" column="ROW_INPUT_DATE"/>
        
        <result property="rowInputIp" column="ROW_INPUT_IP"/>
        
        <result property="rowUpdateEmpNo" column="ROW_UPDATE_EMP_NO"/>
        
        <result property="rowUpdateDate" column="ROW_UPDATE_DATE"/>
        
        <result property="rowUpdateIp" column="ROW_UPDATE_IP"/>
    </resultMap>

    <sql id="simpleMemberClockCondition_">
        <where>
            <include refid="conditions_" />
        </where>
    </sql>

    <sql id="conditions_">
        <include refid="Common.bindAlias"/>
        <if test="memberIds != null and !memberIds.isEmpty()">
            AND ${_alias}MEMBER_ID IN <foreach collection="memberIds" item="memberIdValue" open="(" close=")" separator=",">#{ memberIdValue }</foreach>
        </if>
        <if test="clockDates != null and !clockDates.isEmpty()">
            AND ${_alias}CLOCK_DATE IN <foreach collection="clockDates" item="clockDateValue" open="(" close=")" separator=",">#{ clockDateValue }</foreach>
        </if>
        <if test="memberId != null and memberId != ''">
            AND ${_alias}MEMBER_ID = #{memberId}
        </if>
        <if test="clockDate != null and clockDate.enabled == true">
            <if test="clockDate.from != null and clockDate.from != ''">
            AND ${_alias}CLOCK_DATE >= TO_DATE(#{clockDate.from}, 'YYYY.MM.DD')
            </if>
            <if test="clockDate.to != null and clockDate.to != ''">
            AND ${_alias}CLOCK_DATE <![CDATA[ < ]]> TO_DATE(#{clockDate.to}, 'YYYY.MM.DD') + (INTERVAL '1' DAY)
            </if>
        </if>
        <if test="clockDiv != null and clockDiv != ''">
            AND ${_alias}CLOCK_DIV = #{clockDiv}
        </if>
        <if test="clockYn != null and clockYn != ''">
            AND ${_alias}CLOCK_YN = #{clockYn}
        </if>
        <if test="remark != null and remark != ''">
            AND ${_alias}REMARK = #{remark}
        </if>
        <if test="ip != null and ip != ''">
            AND ${_alias}IP = #{ip}
        </if>
        <if test="rowInputEmpNo != null and rowInputEmpNo != ''">
            AND ${_alias}ROW_INPUT_EMP_NO = #{rowInputEmpNo}
        </if>
        <if test="rowInputDate != null and rowInputDate.enabled == true">
            <if test="rowInputDate.from != null and rowInputDate.from != ''">
            AND ${_alias}ROW_INPUT_DATE >= TO_DATE(#{rowInputDate.from}, 'YYYY.MM.DD')
            </if>
            <if test="rowInputDate.to != null and rowInputDate.to != ''">
            AND ${_alias}ROW_INPUT_DATE <![CDATA[ < ]]> TO_DATE(#{rowInputDate.to}, 'YYYY.MM.DD') + (INTERVAL '1' DAY)
            </if>
        </if>
        <if test="rowInputIp != null and rowInputIp != ''">
            AND ${_alias}ROW_INPUT_IP = #{rowInputIp}
        </if>
        <if test="rowUpdateEmpNo != null and rowUpdateEmpNo != ''">
            AND ${_alias}ROW_UPDATE_EMP_NO = #{rowUpdateEmpNo}
        </if>
        <if test="rowUpdateDate != null and rowUpdateDate != ''">
            AND ${_alias}ROW_UPDATE_DATE = #{rowUpdateDate}
        </if>
        <if test="rowUpdateIp != null and rowUpdateIp != ''">
            AND ${_alias}ROW_UPDATE_IP = #{rowUpdateIp}
        </if>
    </sql>

    <sql id="columns_">
        <include refid="Common.bindAlias"/>
        <include refid="Common.bindAsPrefix"/>

        <choose>
        <when test="'${_asPrefix}' != '' and !'${_asPrefix}'.endsWith('}')">
            ${_alias}MEMBER_ID AS ${_asPrefix}MEMBER_ID, ${_alias}CLOCK_DATE AS ${_asPrefix}CLOCK_DATE, ${_alias}CLOCK_DIV AS ${_asPrefix}CLOCK_DIV, ${_alias}CLOCK_YN AS ${_asPrefix}CLOCK_YN, ${_alias}REMARK AS ${_asPrefix}REMARK, ${_alias}IP AS ${_asPrefix}IP, ${_alias}ROW_INPUT_EMP_NO AS ${_asPrefix}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE AS ${_asPrefix}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP AS ${_asPrefix}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO AS ${_asPrefix}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE AS ${_asPrefix}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP AS ${_asPrefix}ROW_UPDATE_IP        </when>
        <otherwise>
            ${_alias}MEMBER_ID, ${_alias}CLOCK_DATE, ${_alias}CLOCK_DIV, ${_alias}CLOCK_YN, ${_alias}REMARK, ${_alias}IP, ${_alias}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP        </otherwise>
        </choose>
    </sql>

    <sql id="columns_without_id_">
        <include refid="Common.bindAlias"/>
        <include refid="Common.bindAsPrefix"/>

        <choose>
        <when test="'${_asPrefix}' != '' and !'${_asPrefix}'.endsWith('}')">
            ${_alias}CLOCK_DIV AS ${_asPrefix}CLOCK_DIV, ${_alias}CLOCK_YN AS ${_asPrefix}CLOCK_YN, ${_alias}REMARK AS ${_asPrefix}REMARK, ${_alias}IP AS ${_asPrefix}IP, ${_alias}ROW_INPUT_EMP_NO AS ${_asPrefix}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE AS ${_asPrefix}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP AS ${_asPrefix}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO AS ${_asPrefix}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE AS ${_asPrefix}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP AS ${_asPrefix}ROW_UPDATE_IP        </when>
        <otherwise>
            ${_alias}CLOCK_DIV, ${_alias}CLOCK_YN, ${_alias}REMARK, ${_alias}IP, ${_alias}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP        </otherwise>
        </choose>
    </sql>


    <sql id="ins_columns_">
        <include refid="Common.bindAlias"/>
        ${_alias}MEMBER_ID, ${_alias}CLOCK_DATE, ${_alias}CLOCK_DIV, ${_alias}CLOCK_YN, ${_alias}REMARK, ${_alias}IP, ${_alias}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP    </sql>

    <sql id="ins_columns_without_id_">
        <include refid="Common.bindAlias"/>
        ${_alias}CLOCK_DIV, ${_alias}CLOCK_YN, ${_alias}REMARK, ${_alias}IP, ${_alias}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP    </sql>

    <select id="simpleGetMemberClock" parameterType="long" resultMap="simpleMemberClockResult_">
        /* com.neofect.gts.core.services.member.repository.MemberClockRepository_.simpleGetMemberClock */

        SELECT
            <include refid="columns_" />
        FROM
            GTS2016.MEMBER_CLOCK
        WHERE
        MEMBER_ID = #{memberId}
        AND CLOCK_DATE = #{clockDate}
	</select>

    <select id="simpleListMemberClockCnt" resultType="int">
        /* com.neofect.gts.core.services.member.repository.MemberClockRepository_.simpleListMemberClockCnt */

        SELECT COUNT(*)
        FROM GTS2016.MEMBER_CLOCK
        <include refid="simpleMemberClockCondition_" />
    </select>

    <select id="simpleListMemberClock" resultMap="simpleMemberClockResult_">
        /* com.neofect.gts.core.services.member.repository.MemberClockRepository_.simpleListMemberClock */
        <include refid="Common.pagingHeader-oracle"/>
        SELECT
            <include refid="columns_" />
        FROM GTS2016.MEMBER_CLOCK
        <include refid="simpleMemberClockCondition_" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter-oracle"/>
	</select>

    <sql id="update_dynamic_content_">
        <include refid="Common.bindAlias"/>
        <if test="clockDiv != null and clockDiv != ''">
            ${_alias}CLOCK_DIV = #{clockDiv},
        </if>
        <if test="clockYn != null and clockYn != ''">
            ${_alias}CLOCK_YN = #{clockYn},
        </if>
        <if test="remark != null and remark != ''">
            ${_alias}REMARK = #{remark},
        </if>
        <if test="ip != null and ip != ''">
            ${_alias}IP = #{ip},
        </if>
        <if test="rowInputEmpNo != null and rowInputEmpNo != ''">
            ${_alias}ROW_INPUT_EMP_NO = #{rowInputEmpNo},
        </if>
        <if test="rowInputDate != null">
            ${_alias}ROW_INPUT_DATE = #{rowInputDate},
        </if>
        <if test="rowInputIp != null and rowInputIp != ''">
            ${_alias}ROW_INPUT_IP = #{rowInputIp},
        </if>
        <if test="rowUpdateEmpNo != null and rowUpdateEmpNo != ''">
            ${_alias}ROW_UPDATE_EMP_NO = #{rowUpdateEmpNo},
        </if>
        <if test="rowUpdateDate != null and rowUpdateDate != ''">
            ${_alias}ROW_UPDATE_DATE = #{rowUpdateDate},
        </if>
        <if test="rowUpdateIp != null and rowUpdateIp != ''">
            ${_alias}ROW_UPDATE_IP = #{rowUpdateIp},
        </if>
    </sql>

    <update id="updateMemberClock" parameterType="com.neofect.gts.core.services.member.domain.MemberClock">
        /* com.neofect.gts.core.services.member.repository.MemberClockRepository_.updateMemberClock */

        UPDATE GTS2016.MEMBER_CLOCK
        SET
             CLOCK_DIV = #{clockDiv}
            , CLOCK_YN = #{clockYn}
            , REMARK = #{remark}
            , IP = #{ip}
            , ROW_INPUT_EMP_NO = #{rowInputEmpNo}
            , ROW_INPUT_DATE = #{rowInputDate}
            , ROW_INPUT_IP = #{rowInputIp}
            , ROW_UPDATE_EMP_NO = #{rowUpdateEmpNo}
            , ROW_UPDATE_DATE = #{rowUpdateDate}
            , ROW_UPDATE_IP = #{rowUpdateIp}
        WHERE
            MEMBER_ID = #{memberId}
            AND CLOCK_DATE = #{clockDate}
    </update>

    <update id="updateMemberClockDynamic" parameterType="com.neofect.gts.core.services.member.domain.MemberClock">
        /* com.neofect.gts.core.services.member.repository.MemberClockRepository_.updateMemberClockDynamic */

        UPDATE GTS2016.MEMBER_CLOCK
            <set>
            <include refid="update_dynamic_content_" />
            </set>
        WHERE
            MEMBER_ID = #{memberId}
            AND CLOCK_DATE = #{clockDate}
    </update>

    <delete id="deleteMemberClock" parameterType="long">
        /* com.neofect.gts.core.services.member.repository.MemberClockRepository_.deleteMemberClock */

        DELETE FROM GTS2016.MEMBER_CLOCK
        WHERE
            MEMBER_ID = #{memberId}
            AND CLOCK_DATE = #{clockDate}
    </delete>

    <sql id="insert_values_">

        #{clockDiv},
        #{clockYn},
        #{remark},
        #{ip},
        #{rowInputEmpNo},
        #{rowInputDate},
        #{rowInputIp},
        #{rowUpdateEmpNo},
        #{rowUpdateDate},
        #{rowUpdateIp}
    </sql>

    <insert id="insertMemberClock">
        /* com.neofect.gts.core.services.member.repository.MemberClockRepository_.insertMemberClock */


        INSERT INTO GTS2016.MEMBER_CLOCK
        (
        <include refid="ins_columns_without_id_" />
        )
        VALUES (
        <include refid="insert_values_" />
        )
        <selectKey resultType="java.lang.Long" keyProperty="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>

	</insert>

</mapper>

