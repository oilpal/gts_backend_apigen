<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/MybatisBase.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.sm.repository.SumMmRepository_">

    <resultMap id="simpleSumMmResult_" type="com.neofect.gts.core.services.sm.domain.SumMm">
        
        <id     property="deptId" column="DEPT_ID"/>
        
        <id     property="yyyymm" column="YYYYMM"/>
        
        <id     property="acntCode" column="ACNT_CODE"/>
        <!-- 1:예정 9:확정 -->
        <id     property="status" column="STATUS"/>
        
        <result property="amt" column="AMT"/>
    </resultMap>

    <sql id="simpleSumMmCondition_">
        <where>
            <include refid="conditions_" />
        </where>
    </sql>

    <sql id="conditions_">
        <include refid="Common.bindAlias"/>
        <if test="deptIds != null and !deptIds.isEmpty()">
            AND ${_alias}DEPT_ID IN <foreach collection="deptIds" item="deptIdValue" open="(" close=")" separator=",">#{ deptIdValue }</foreach>
        </if>
        <if test="yyyymms != null and !yyyymms.isEmpty()">
            AND ${_alias}YYYYMM IN <foreach collection="yyyymms" item="yyyymmValue" open="(" close=")" separator=",">#{ yyyymmValue }</foreach>
        </if>
        <if test="acntCodes != null and !acntCodes.isEmpty()">
            AND ${_alias}ACNT_CODE IN <foreach collection="acntCodes" item="acntCodeValue" open="(" close=")" separator=",">#{ acntCodeValue }</foreach>
        </if>
        <if test="statuss != null and !statuss.isEmpty()">
            AND ${_alias}STATUS IN <foreach collection="statuss" item="statusValue" open="(" close=")" separator=",">#{ statusValue }</foreach>
        </if>
        <if test="deptId != null and deptId != ''">
            AND ${_alias}DEPT_ID = #{deptId}
        </if>
        <if test="yyyymm != null and yyyymm != ''">
            AND ${_alias}YYYYMM = #{yyyymm}
        </if>
        <if test="acntCode != null and acntCode != ''">
            AND ${_alias}ACNT_CODE = #{acntCode}
        </if>
        <if test="amt != null">
            AND ${_alias}AMT = #{amt}
        </if>
        <if test="status != null and status != ''">
            AND ${_alias}STATUS = #{status}
        </if>
    </sql>

    <sql id="columns_">
        <include refid="Common.bindAlias"/>
        <include refid="Common.bindAsPrefix"/>

        <choose>
        <when test="'${_asPrefix}' != '' and !'${_asPrefix}'.endsWith('}')">
            ${_alias}DEPT_ID AS ${_asPrefix}DEPT_ID, ${_alias}YYYYMM AS ${_asPrefix}YYYYMM, ${_alias}ACNT_CODE AS ${_asPrefix}ACNT_CODE, ${_alias}AMT AS ${_asPrefix}AMT, ${_alias}STATUS AS ${_asPrefix}STATUS        </when>
        <otherwise>
            ${_alias}DEPT_ID, ${_alias}YYYYMM, ${_alias}ACNT_CODE, ${_alias}AMT, ${_alias}STATUS        </otherwise>
        </choose>
    </sql>

    <sql id="columns_without_id_">
        <include refid="Common.bindAlias"/>
        <include refid="Common.bindAsPrefix"/>

        <choose>
        <when test="'${_asPrefix}' != '' and !'${_asPrefix}'.endsWith('}')">
            ${_alias}AMT AS ${_asPrefix}AMT        </when>
        <otherwise>
            ${_alias}AMT        </otherwise>
        </choose>
    </sql>


    <sql id="ins_columns_">
        <include refid="Common.bindAlias"/>
        ${_alias}DEPT_ID, ${_alias}YYYYMM, ${_alias}ACNT_CODE, ${_alias}AMT, ${_alias}STATUS    </sql>

    <sql id="ins_columns_without_id_">
        <include refid="Common.bindAlias"/>
        ${_alias}AMT    </sql>

    <select id="simpleGetSumMm" parameterType="long" resultMap="simpleSumMmResult_">
        /* com.neofect.gts.core.services.sm.repository.SumMmRepository_.simpleGetSumMm */

        SELECT
            <include refid="columns_" />
        FROM
            GTS2016.SUM_MM
        WHERE
        DEPT_ID = #{deptId}
        AND YYYYMM = #{yyyymm}
        AND ACNT_CODE = #{acntCode}
        AND STATUS = #{status}
	</select>

    <select id="simpleListSumMmCnt" resultType="int">
        /* com.neofect.gts.core.services.sm.repository.SumMmRepository_.simpleListSumMmCnt */

        SELECT COUNT(*)
        FROM GTS2016.SUM_MM
        <include refid="simpleSumMmCondition_" />
    </select>

    <select id="simpleListSumMm" resultMap="simpleSumMmResult_">
        /* com.neofect.gts.core.services.sm.repository.SumMmRepository_.simpleListSumMm */
        <include refid="Common.pagingHeader-oracle"/>
        SELECT
            <include refid="columns_" />
        FROM GTS2016.SUM_MM
        <include refid="simpleSumMmCondition_" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter-oracle"/>
	</select>

    <sql id="update_dynamic_content_">
        <include refid="Common.bindAlias"/>
        <if test="amt != null">
            ${_alias}AMT = #{amt},
        </if>
    </sql>

    <update id="updateSumMm" parameterType="com.neofect.gts.core.services.sm.domain.SumMm">
        /* com.neofect.gts.core.services.sm.repository.SumMmRepository_.updateSumMm */

        UPDATE GTS2016.SUM_MM
        SET
             AMT = #{amt}
        WHERE
            DEPT_ID = #{deptId}
            AND YYYYMM = #{yyyymm}
            AND ACNT_CODE = #{acntCode}
            AND STATUS = #{status}
    </update>

    <update id="updateSumMmDynamic" parameterType="com.neofect.gts.core.services.sm.domain.SumMm">
        /* com.neofect.gts.core.services.sm.repository.SumMmRepository_.updateSumMmDynamic */

        UPDATE GTS2016.SUM_MM
            <set>
            <include refid="update_dynamic_content_" />
            </set>
        WHERE
            DEPT_ID = #{deptId}
            AND YYYYMM = #{yyyymm}
            AND ACNT_CODE = #{acntCode}
            AND STATUS = #{status}
    </update>

    <delete id="deleteSumMm" parameterType="long">
        /* com.neofect.gts.core.services.sm.repository.SumMmRepository_.deleteSumMm */

        DELETE FROM GTS2016.SUM_MM
        WHERE
            DEPT_ID = #{deptId}
            AND YYYYMM = #{yyyymm}
            AND ACNT_CODE = #{acntCode}
            AND STATUS = #{status}
    </delete>

    <sql id="insert_values_">

        #{amt}
    </sql>

    <insert id="insertSumMm">
        /* com.neofect.gts.core.services.sm.repository.SumMmRepository_.insertSumMm */


        INSERT INTO GTS2016.SUM_MM
        (
        <include refid="ins_columns_without_id_" />
        )
        VALUES (
        <include refid="insert_values_" />
        )
        <selectKey resultType="java.lang.Long" keyProperty="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>

	</insert>

</mapper>

