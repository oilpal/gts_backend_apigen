<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/MybatisBase.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.common.repository.CodeCardRepository_">

    <resultMap id="simpleCodeCardResult_" type="com.neofect.gts.core.services.common.domain.CodeCard">
        <!-- 카드번호 -->
        <id     property="id" column="CARD_NO"/>
        <!-- 카드회사코드 -->
        <result property="cardDiv" column="CARD_DIV"/>
        <!-- 결제계좌번호 -->
        <result property="depositno" column="DEPOSITNO"/>
        <!-- 월결제일 -->
        <result property="dd" column="DD"/>
        <!-- 유효기간 -->
        <result property="yyyymm" column="YYYYMM"/>
        <!-- 사용여부 -->
        <result property="useYn" column="USE_YN"/>
        <!-- 지사ID -->
        <result property="deptId" column="DEPT_ID"/>
        <!-- 사번 -->
        <result property="memberId" column="MEMBER_ID"/>
        
        <result property="rowInputEmpNo" column="ROW_INPUT_EMP_NO"/>
        
        <result property="rowInputDate" column="ROW_INPUT_DATE"/>
        
        <result property="rowInputIp" column="ROW_INPUT_IP"/>
        
        <result property="rowUpdateEmpNo" column="ROW_UPDATE_EMP_NO"/>
        
        <result property="rowUpdateDate" column="ROW_UPDATE_DATE"/>
        
        <result property="rowUpdateIp" column="ROW_UPDATE_IP"/>
    </resultMap>

    <sql id="simpleCodeCardCondition_">
        <where>
            <include refid="conditions_" />
        </where>
    </sql>

    <sql id="conditions_">
        <include refid="Common.bindAlias"/>
        <if test="ids != null and !ids.isEmpty()">
            AND ${_alias}CARD_NO IN <foreach collection="ids" item="idValue" open="(" close=")" separator=",">#{ idValue }</foreach>
        </if>
        <if test="id != null and id != ''">
            AND ${_alias}CARD_NO = #{id}
        </if>
        <if test="cardDiv != null and cardDiv != ''">
            AND ${_alias}CARD_DIV = #{cardDiv}
        </if>
        <if test="depositno != null and depositno != ''">
            AND ${_alias}DEPOSITNO = #{depositno}
        </if>
        <if test="dd != null and dd != ''">
            AND ${_alias}DD = #{dd}
        </if>
        <if test="yyyymm != null and yyyymm != ''">
            AND ${_alias}YYYYMM = #{yyyymm}
        </if>
        <if test="useYn != null and useYn != ''">
            AND ${_alias}USE_YN = #{useYn}
        </if>
        <if test="deptId != null and deptId != ''">
            AND ${_alias}DEPT_ID = #{deptId}
        </if>
        <if test="memberId != null and memberId != ''">
            AND ${_alias}MEMBER_ID = #{memberId}
        </if>
        <if test="rowInputEmpNo != null and rowInputEmpNo != ''">
            AND ${_alias}ROW_INPUT_EMP_NO = #{rowInputEmpNo}
        </if>
        <if test="rowInputDate != null and rowInputDate.enabled == true">
            <if test="rowInputDate.from != null and rowInputDate.from != ''">
            AND ${_alias}ROW_INPUT_DATE >= TO_DATE(#{rowInputDate.from}, 'YYYY.MM.DD')
            </if>
            <if test="rowInputDate.to != null and rowInputDate.to != ''">
            AND ${_alias}ROW_INPUT_DATE <![CDATA[ < ]]> TO_DATE(#{rowInputDate.to}, 'YYYY.MM.DD') + (INTERVAL '1' DAY)
            </if>
        </if>
        <if test="rowInputIp != null and rowInputIp != ''">
            AND ${_alias}ROW_INPUT_IP = #{rowInputIp}
        </if>
        <if test="rowUpdateEmpNo != null and rowUpdateEmpNo != ''">
            AND ${_alias}ROW_UPDATE_EMP_NO = #{rowUpdateEmpNo}
        </if>
        <if test="rowUpdateDate != null and rowUpdateDate != ''">
            AND ${_alias}ROW_UPDATE_DATE = #{rowUpdateDate}
        </if>
        <if test="rowUpdateIp != null and rowUpdateIp != ''">
            AND ${_alias}ROW_UPDATE_IP = #{rowUpdateIp}
        </if>
    </sql>

    <sql id="columns_">
        <include refid="Common.bindAlias"/>
        <include refid="Common.bindAsPrefix"/>

        <choose>
        <when test="'${_asPrefix}' != '' and !'${_asPrefix}'.endsWith('}')">
            ${_alias}CARD_NO AS ${_asPrefix}CARD_NO, ${_alias}CARD_DIV AS ${_asPrefix}CARD_DIV, ${_alias}DEPOSITNO AS ${_asPrefix}DEPOSITNO, ${_alias}DD AS ${_asPrefix}DD, ${_alias}YYYYMM AS ${_asPrefix}YYYYMM, ${_alias}USE_YN AS ${_asPrefix}USE_YN, ${_alias}DEPT_ID AS ${_asPrefix}DEPT_ID, ${_alias}MEMBER_ID AS ${_asPrefix}MEMBER_ID, ${_alias}ROW_INPUT_EMP_NO AS ${_asPrefix}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE AS ${_asPrefix}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP AS ${_asPrefix}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO AS ${_asPrefix}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE AS ${_asPrefix}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP AS ${_asPrefix}ROW_UPDATE_IP        </when>
        <otherwise>
            ${_alias}CARD_NO, ${_alias}CARD_DIV, ${_alias}DEPOSITNO, ${_alias}DD, ${_alias}YYYYMM, ${_alias}USE_YN, ${_alias}DEPT_ID, ${_alias}MEMBER_ID, ${_alias}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP        </otherwise>
        </choose>
    </sql>

    <sql id="columns_without_id_">
        <include refid="Common.bindAlias"/>
        <include refid="Common.bindAsPrefix"/>

        <choose>
        <when test="'${_asPrefix}' != '' and !'${_asPrefix}'.endsWith('}')">
            ${_alias}CARD_DIV AS ${_asPrefix}CARD_DIV, ${_alias}DEPOSITNO AS ${_asPrefix}DEPOSITNO, ${_alias}DD AS ${_asPrefix}DD, ${_alias}YYYYMM AS ${_asPrefix}YYYYMM, ${_alias}USE_YN AS ${_asPrefix}USE_YN, ${_alias}DEPT_ID AS ${_asPrefix}DEPT_ID, ${_alias}MEMBER_ID AS ${_asPrefix}MEMBER_ID, ${_alias}ROW_INPUT_EMP_NO AS ${_asPrefix}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE AS ${_asPrefix}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP AS ${_asPrefix}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO AS ${_asPrefix}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE AS ${_asPrefix}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP AS ${_asPrefix}ROW_UPDATE_IP        </when>
        <otherwise>
            ${_alias}CARD_DIV, ${_alias}DEPOSITNO, ${_alias}DD, ${_alias}YYYYMM, ${_alias}USE_YN, ${_alias}DEPT_ID, ${_alias}MEMBER_ID, ${_alias}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP        </otherwise>
        </choose>
    </sql>


    <sql id="ins_columns_">
        <include refid="Common.bindAlias"/>
        ${_alias}CARD_NO, ${_alias}CARD_DIV, ${_alias}DEPOSITNO, ${_alias}DD, ${_alias}YYYYMM, ${_alias}USE_YN, ${_alias}DEPT_ID, ${_alias}MEMBER_ID, ${_alias}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP    </sql>

    <sql id="ins_columns_without_id_">
        <include refid="Common.bindAlias"/>
        ${_alias}CARD_DIV, ${_alias}DEPOSITNO, ${_alias}DD, ${_alias}YYYYMM, ${_alias}USE_YN, ${_alias}DEPT_ID, ${_alias}MEMBER_ID, ${_alias}ROW_INPUT_EMP_NO, ${_alias}ROW_INPUT_DATE, ${_alias}ROW_INPUT_IP, ${_alias}ROW_UPDATE_EMP_NO, ${_alias}ROW_UPDATE_DATE, ${_alias}ROW_UPDATE_IP    </sql>

    <select id="simpleGetCodeCard" parameterType="long" resultMap="simpleCodeCardResult_">
        /* com.neofect.gts.core.services.common.repository.CodeCardRepository_.simpleGetCodeCard */

        SELECT
            <include refid="columns_" />
        FROM
            GTS2016.CODE_CARD
        WHERE
        CARD_NO = #{id}
	</select>

    <select id="simpleListCodeCardCnt" resultType="int">
        /* com.neofect.gts.core.services.common.repository.CodeCardRepository_.simpleListCodeCardCnt */

        SELECT COUNT(*)
        FROM GTS2016.CODE_CARD
        <include refid="simpleCodeCardCondition_" />
    </select>

    <select id="simpleListCodeCard" resultMap="simpleCodeCardResult_">
        /* com.neofect.gts.core.services.common.repository.CodeCardRepository_.simpleListCodeCard */
        <include refid="Common.pagingHeader-oracle"/>
        SELECT
            <include refid="columns_" />
        FROM GTS2016.CODE_CARD
        <include refid="simpleCodeCardCondition_" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter-oracle"/>
	</select>

    <sql id="update_dynamic_content_">
        <include refid="Common.bindAlias"/>
        <if test="cardDiv != null and cardDiv != ''">
            ${_alias}CARD_DIV = #{cardDiv},
        </if>
        <if test="depositno != null and depositno != ''">
            ${_alias}DEPOSITNO = #{depositno},
        </if>
        <if test="dd != null and dd != ''">
            ${_alias}DD = #{dd},
        </if>
        <if test="yyyymm != null and yyyymm != ''">
            ${_alias}YYYYMM = #{yyyymm},
        </if>
        <if test="useYn != null and useYn != ''">
            ${_alias}USE_YN = #{useYn},
        </if>
        <if test="deptId != null and deptId != ''">
            ${_alias}DEPT_ID = #{deptId},
        </if>
        <if test="memberId != null and memberId != ''">
            ${_alias}MEMBER_ID = #{memberId},
        </if>
        <if test="rowInputEmpNo != null and rowInputEmpNo != ''">
            ${_alias}ROW_INPUT_EMP_NO = #{rowInputEmpNo},
        </if>
        <if test="rowInputDate != null">
            ${_alias}ROW_INPUT_DATE = #{rowInputDate},
        </if>
        <if test="rowInputIp != null and rowInputIp != ''">
            ${_alias}ROW_INPUT_IP = #{rowInputIp},
        </if>
        <if test="rowUpdateEmpNo != null and rowUpdateEmpNo != ''">
            ${_alias}ROW_UPDATE_EMP_NO = #{rowUpdateEmpNo},
        </if>
        <if test="rowUpdateDate != null and rowUpdateDate != ''">
            ${_alias}ROW_UPDATE_DATE = #{rowUpdateDate},
        </if>
        <if test="rowUpdateIp != null and rowUpdateIp != ''">
            ${_alias}ROW_UPDATE_IP = #{rowUpdateIp},
        </if>
    </sql>

    <update id="updateCodeCard" parameterType="com.neofect.gts.core.services.common.domain.CodeCard">
        /* com.neofect.gts.core.services.common.repository.CodeCardRepository_.updateCodeCard */

        UPDATE GTS2016.CODE_CARD
        SET
             CARD_DIV = #{cardDiv}
            , DEPOSITNO = #{depositno}
            , DD = #{dd}
            , YYYYMM = #{yyyymm}
            , USE_YN = #{useYn}
            , DEPT_ID = #{deptId}
            , MEMBER_ID = #{memberId}
            , ROW_INPUT_EMP_NO = #{rowInputEmpNo}
            , ROW_INPUT_DATE = #{rowInputDate}
            , ROW_INPUT_IP = #{rowInputIp}
            , ROW_UPDATE_EMP_NO = #{rowUpdateEmpNo}
            , ROW_UPDATE_DATE = #{rowUpdateDate}
            , ROW_UPDATE_IP = #{rowUpdateIp}
        WHERE
            CARD_NO = #{id}
    </update>

    <update id="updateCodeCardDynamic" parameterType="com.neofect.gts.core.services.common.domain.CodeCard">
        /* com.neofect.gts.core.services.common.repository.CodeCardRepository_.updateCodeCardDynamic */

        UPDATE GTS2016.CODE_CARD
            <set>
            <include refid="update_dynamic_content_" />
            </set>
        WHERE
            CARD_NO = #{id}
    </update>

    <delete id="deleteCodeCard" parameterType="long">
        /* com.neofect.gts.core.services.common.repository.CodeCardRepository_.deleteCodeCard */

        DELETE FROM GTS2016.CODE_CARD
        WHERE
            CARD_NO = #{id}
    </delete>

    <sql id="insert_values_">

        #{cardDiv},
        #{depositno},
        #{dd},
        #{yyyymm},
        #{useYn},
        #{deptId},
        #{memberId},
        #{rowInputEmpNo},
        #{rowInputDate},
        #{rowInputIp},
        #{rowUpdateEmpNo},
        #{rowUpdateDate},
        #{rowUpdateIp}
    </sql>

    <insert id="insertCodeCard">
        /* com.neofect.gts.core.services.common.repository.CodeCardRepository_.insertCodeCard */


        INSERT INTO GTS2016.CODE_CARD
        (
        <include refid="ins_columns_without_id_" />
        )
        VALUES (
        <include refid="insert_values_" />
        )
        <selectKey resultType="java.lang.Long" keyProperty="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>

	</insert>

</mapper>

