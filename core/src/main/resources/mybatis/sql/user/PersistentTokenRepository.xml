<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/Mybatis.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.user.repository.PersistentTokenRepository">
    <!--
        <include refid="columns" /> 대신 <include refid="columns"><property name="_alias" value="b."/></include> 방식으로 alias 를 줄 수 있습니다.
    -->
    <resultMap id="simplePersistentTokenResult" type="persistentToken" extends="com.neofect.gts.core.services.user.repository.PersistentTokenRepository_.simplePersistentTokenResult_">

    </resultMap>

    <sql id="persistentTokenCondition">
        <where>
            <include refid="com.neofect.gts.core.services.user.repository.PersistentTokenRepository_.conditions_" />
        </where>
    </sql>

    <sql id="columns">
        <include refid="com.neofect.gts.core.services.user.repository.PersistentTokenRepository_.columns_" />
    </sql>

    <select id="getPersistentToken" parameterType="long" resultMap="simplePersistentTokenResult">
        /* PersistentTokenRepository.getPersistentToken */

        SELECT
            <include refid="columns" />
        FROM
            nv_persistent_token
        WHERE
        pt_id = #{id}
	</select>

    <select id="listPersistentTokenCnt" resultType="int">
        /* PersistentTokenRepository.listPersistentTokenCnt */

        SELECT COUNT(DISTINCT pt_id )
        FROM nv_persistent_token
        <include refid="persistentTokenCondition" />
    </select>

    <select id="listPersistentToken" resultMap="simplePersistentTokenResult">
        /* PersistentTokenRepository.listPersistentToken */
        SELECT
            <include refid="columns" />
        FROM nv_persistent_token
        <include refid="persistentTokenCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
	</select>

    <select id="listPersistentTokenIds" resultType="long">
        /* PersistentTokenRepository.listPersistentTokenIds */
        SELECT DISTINCT pt_id  <include refid="Common.sortKeyList"/>
        FROM nv_persistent_token
        <include refid="persistentTokenCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
    </select>

    <select id="getPersistentTokenBySeries" resultMap="simplePersistentTokenResult">
        /* PersistentTokenRepository.getPersistentTokenBySeries */

        SELECT
            <include refid="columns" />
        FROM nv_persistent_token
        WHERE pt_series = #{series}
    </select>

    <delete id="deletePersistentTokenBySeries" parameterType="string">
        /* com.unvus.iflex.core.services.user.repository.PersistentTokenRepository.deletePersistentTokenBySeries */

        DELETE FROM nv_persistent_token
        WHERE
            pt_series = #{series}
    </delete>

</mapper>

