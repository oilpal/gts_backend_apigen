<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/Mybatis.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.user.repository.TermAgreeRepository">
    <!--region ====================================================================================== reference only -->
    <resultMap id="_metaInfo" type="java.util.Map">
        <!-- Generated Files -->
        <result property="model_" column="com.neofect.gts.core.services.user.domain.TermAgree_"/>
        <result property="repository_" column="com.neofect.gts.core.services.user.repository.TermAgreeRepository_"/>
        <!-- Extended Files -->
        <result property="model" column="com.neofect.gts.core.services.user.domain.TermAgree"/>
        <result property="repository" column="com.neofect.gts.core.services.user.repository.TermAgreeRepository"/>
    </resultMap>

    <!--
     ! All Columns
     !  ta_acnt_id, ta_term_code, ta_agree, ta_modified_dt, ta_modified_by
    -->

    <!--
        <include refid="columns" /> 대신 <include refid="columns"><property name="_alias" value="b."/></include> 방식으로 alias 를 줄 수 있습니다.
    -->

    <!--endregion =================================================================================== reference only -->

    <resultMap id="simpleTermAgreeResult" type="termAgree" extends="com.neofect.gts.core.services.user.repository.TermAgreeRepository_.simpleTermAgreeResult_">

    </resultMap>

    <sql id="termAgreeConditionEx">
        <!--
            add your 'termAgree's condition here
         -->
        <include refid="com.neofect.gts.core.services.user.repository.TermAgreeRepository_.conditions_" />
    </sql>

    <sql id="termAgreeCondition">
        <where>
            <include refid="termAgreeConditionEx" />
            <!--
                add your 'joined table's condition here
             -->
        </where>
    </sql>

    <sql id="columns">
        <include refid="com.neofect.gts.core.services.user.repository.TermAgreeRepository_.columns_" />
    </sql>

    <select id="getTermAgree" parameterType="long" resultMap="simpleTermAgreeResult">
        /* TermAgreeRepository.getTermAgree */

        SELECT
            <include refid="columns" />
        FROM
            nv_term_agree
        WHERE
        ta_acnt_id = #{acntId}
        AND ta_term_code = #{termCode}
	</select>

    <select id="listTermAgreeCnt" resultType="int">
        /* TermAgreeRepository.listTermAgreeCnt */

        SELECT COUNT(DISTINCT ta_acnt_id ta_term_code )
        FROM nv_term_agree
        <include refid="termAgreeCondition" />
    </select>

    <select id="listTermAgree" resultMap="simpleTermAgreeResult">
        /* TermAgreeRepository.listTermAgree */
        SELECT
            <include refid="columns" />
        FROM nv_term_agree
        <include refid="termAgreeCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
	</select>

    <select id="listTermAgreeIds" resultType="long">
        /* TermAgreeRepository.listTermAgreeIds */
        SELECT DISTINCT ta_acnt_id ta_term_code  <include refid="Common.sortKeyList"/>
        FROM nv_term_agree
        <include refid="termAgreeCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
    </select>


</mapper>

