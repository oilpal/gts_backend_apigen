<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/Mybatis.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.attach.repository.AttachRepository">
    <!--
        <include refid="columns" /> 대신 <include refid="columns"><property name="_alias" value="b."/></include> 방식으로 alias 를 줄 수 있습니다.
    -->
    <resultMap id="simpleAttachResult" type="attach" extends="com.neofect.gts.core.services.attach.repository.AttachRepository_.simpleAttachResult_">

    </resultMap>

    <sql id="attachCondition">
        <where>
            <include refid="com.neofect.gts.core.services.attach.repository.AttachRepository_.conditions_" />
        </where>
    </sql>

    <sql id="columns">
        <include refid="com.neofect.gts.core.services.attach.repository.AttachRepository_.columns_" />
    </sql>

    <select id="getAttach" parameterType="long" resultMap="simpleAttachResult">
        /* AttachRepository.getAttach */

        SELECT
            <include refid="columns" />
        FROM
            nv_attach
        WHERE
        attach_id = #{id}
	</select>

    <select id="listAttachCnt" resultType="int">
        /* AttachRepository.listAttachCnt */

        SELECT COUNT(DISTINCT attach_id )
        FROM nv_attach
        <include refid="attachCondition" />
    </select>

    <select id="listAttach" resultMap="simpleAttachResult">
        /* AttachRepository.listAttach */
        SELECT
            <include refid="columns" />
        FROM nv_attach
        <include refid="attachCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
	</select>

    <select id="listAttachIds" resultType="long">
        /* AttachRepository.listAttachIds */
        SELECT DISTINCT attach_id  <include refid="Common.sortKeyList"/>
        FROM nv_attach
        <include refid="attachCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
    </select>

    <update id="markAsDelete">
        UPDATE nv_attach
        SET
            attach_deleted = true
        WHERE
            attach_id = #{id}
    </update>

</mapper>

