<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/Mybatis.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.common.repository.AddrRepository">
    <!--
        <include refid="columns" /> 대신 <include refid="columns"><property name="_alias" value="b."/></include> 방식으로 alias 를 줄 수 있습니다.
    -->
    <resultMap id="simpleAddrResult" type="addr" extends="com.neofect.gts.core.services.common.repository.AddrRepository_.simpleAddrResult_">

    </resultMap>

    <sql id="addrCondition">
        <where>
            <include refid="com.neofect.gts.core.services.common.repository.AddrRepository_.conditions_" />
        </where>
    </sql>

    <sql id="columns">
        <include refid="com.neofect.gts.core.services.common.repository.AddrRepository_.columns_" />
    </sql>

    <select id="getAddr" parameterType="long" resultMap="simpleAddrResult">
        /* AddrRepository.getAddr */

        SELECT
            <include refid="columns" />
        FROM
            nv_addr
        WHERE
        addr_id = #{id}
	</select>

    <select id="listAddrCnt" resultType="int">
        /* AddrRepository.listAddrCnt */

        SELECT COUNT(DISTINCT addr_id )
        FROM nv_addr
        <include refid="addrCondition" />
    </select>

    <select id="listAddr" resultMap="simpleAddrResult">
        /* AddrRepository.listAddr */
        SELECT
            <include refid="columns" />
        FROM nv_addr
        <include refid="addrCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
	</select>

    <select id="listAddrIds" resultType="long">
        /* AddrRepository.listAddrIds */
        SELECT DISTINCT addr_id  <include refid="Common.sortKeyList"/>
        FROM nv_addr
        <include refid="addrCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
    </select>


</mapper>

