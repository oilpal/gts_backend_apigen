<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/Mybatis.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.common.repository.LabelValueRepository">
    <!--
        <include refid="columns" /> 대신 <include refid="columns"><property name="_alias" value="b."/></include> 방식으로 alias 를 줄 수 있습니다.
    -->
    <resultMap id="simpleLabelValueResult" type="labelValue" extends="com.neofect.gts.core.services.common.repository.LabelValueRepository_.simpleLabelValueResult_">

    </resultMap>

    <sql id="labelValueCondition">
        <where>
            <include refid="com.neofect.gts.core.services.common.repository.LabelValueRepository_.conditions_" />
        </where>
    </sql>

    <sql id="columns">
        <include refid="com.neofect.gts.core.services.common.repository.LabelValueRepository_.columns_" />
    </sql>

    <select id="getLabelValue" parameterType="long" resultMap="simpleLabelValueResult">
        /* LabelValueRepository.getLabelValue */

        SELECT
            <include refid="columns" />
        FROM
            nv_label_value
        WHERE
        lv_id = #{id}
	</select>

    <select id="listLabelValueCnt" resultType="int">
        /* LabelValueRepository.listLabelValueCnt */

        SELECT COUNT(DISTINCT lv_id )
        FROM nv_label_value
        <include refid="labelValueCondition" />
    </select>

    <select id="listLabelValue" resultMap="simpleLabelValueResult">
        /* LabelValueRepository.listLabelValue */
        SELECT
            <include refid="columns" />
        FROM nv_label_value
        <include refid="labelValueCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
	</select>

    <select id="listLabelValueIds" resultType="long">
        /* LabelValueRepository.listLabelValueIds */
        SELECT DISTINCT lv_id  <include refid="Common.sortKeyList"/>
        FROM nv_label_value
        <include refid="labelValueCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
    </select>


</mapper>

