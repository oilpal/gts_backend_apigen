<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/Mybatis.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.common.repository.ActionLogRepository">
    <!--
        <include refid="columns" /> 대신 <include refid="columns"><property name="_alias" value="b."/></include> 방식으로 alias 를 줄 수 있습니다.
    -->
    <resultMap id="simpleActionLogResult" type="actionLog" extends="com.neofect.gts.core.services.common.repository.ActionLogRepository_.simpleActionLogResult_">

    </resultMap>

    <sql id="actionLogCondition">
        <where>
            <include refid="com.neofect.gts.core.services.common.repository.ActionLogRepository_.conditions_" />
        </where>
    </sql>

    <sql id="columns">
        <include refid="com.neofect.gts.core.services.common.repository.ActionLogRepository_.columns_" />
    </sql>

    <select id="getActionLog" parameterType="long" resultMap="simpleActionLogResult">
        /* ActionLogRepository.getActionLog */

        SELECT
            <include refid="columns" />
        FROM
            nv_action_log
        WHERE
        al_id = #{id}
	</select>

    <select id="listActionLogCnt" resultType="int">
        /* ActionLogRepository.listActionLogCnt */

        SELECT COUNT(DISTINCT al_id )
        FROM nv_action_log
        <include refid="actionLogCondition" />
    </select>

    <select id="listActionLog" resultMap="simpleActionLogResult">
        /* ActionLogRepository.listActionLog */
        SELECT
            <include refid="columns" />
        FROM nv_action_log
        <include refid="actionLogCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
	</select>

    <select id="listActionLogIds" resultType="long">
        /* ActionLogRepository.listActionLogIds */
        SELECT DISTINCT al_id  <include refid="Common.sortKeyList"/>
        FROM nv_action_log
        <include refid="actionLogCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
    </select>


</mapper>

