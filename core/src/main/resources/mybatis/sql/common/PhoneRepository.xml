<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/Mybatis.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.common.repository.PhoneRepository">
    <!--
        <include refid="columns" /> 대신 <include refid="columns"><property name="_alias" value="b."/></include> 방식으로 alias 를 줄 수 있습니다.
    -->
    <resultMap id="simplePhoneResult" type="phone" extends="com.neofect.gts.core.services.common.repository.PhoneRepository_.simplePhoneResult_">

    </resultMap>

    <sql id="phoneCondition">
        <where>
            <include refid="com.neofect.gts.core.services.common.repository.PhoneRepository_.conditions_" />
        </where>
    </sql>

    <sql id="columns">
        <include refid="com.neofect.gts.core.services.common.repository.PhoneRepository_.columns_" />
    </sql>

    <select id="getPhone" parameterType="long" resultMap="simplePhoneResult">
        /* PhoneRepository.getPhone */

        SELECT
            <include refid="columns" />
        FROM
            nv_phone
        WHERE
        phone_id = #{id}
	</select>

    <select id="listPhoneCnt" resultType="int">
        /* PhoneRepository.listPhoneCnt */

        SELECT COUNT(DISTINCT phone_id )
        FROM nv_phone
        <include refid="phoneCondition" />
    </select>

    <select id="listPhone" resultMap="simplePhoneResult">
        /* PhoneRepository.listPhone */
        SELECT
            <include refid="columns" />
        FROM nv_phone
        <include refid="phoneCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
	</select>

    <select id="listPhoneIds" resultType="long">
        /* PhoneRepository.listPhoneIds */
        SELECT DISTINCT phone_id  <include refid="Common.sortKeyList"/>
        FROM nv_phone
        <include refid="phoneCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
    </select>


</mapper>

