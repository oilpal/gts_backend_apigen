<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/Mybatis.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.site.repository.SiteConfigRepository">
    <!--
        <include refid="columns" /> 대신 <include refid="columns"><property name="_alias" value="b."/></include> 방식으로 alias 를 줄 수 있습니다.
    -->
    <resultMap id="simpleSiteConfigResult" type="siteConfig" extends="com.neofect.gts.core.services.site.repository.SiteConfigRepository_.simpleSiteConfigResult_">

    </resultMap>

    <sql id="siteConfigCondition">
        <where>
            <include refid="com.neofect.gts.core.services.site.repository.SiteConfigRepository_.conditions_" />
        </where>
    </sql>

    <sql id="columns">
        <include refid="com.neofect.gts.core.services.site.repository.SiteConfigRepository_.columns_" />
    </sql>

    <select id="getSiteConfig" parameterType="long" resultMap="simpleSiteConfigResult">
        /* SiteConfigRepository.getSiteConfig */

        SELECT
            <include refid="columns" />
        FROM
            nv_site_config
        WHERE
        sc_id = #{id}
	</select>

    <select id="listSiteConfigCnt" resultType="int">
        /* SiteConfigRepository.listSiteConfigCnt */

        SELECT COUNT(DISTINCT sc_id )
        FROM nv_site_config
        <include refid="siteConfigCondition" />
    </select>

    <select id="listSiteConfig" resultMap="simpleSiteConfigResult">
        /* SiteConfigRepository.listSiteConfig */
        SELECT
            <include refid="columns" />
        FROM nv_site_config
        <include refid="siteConfigCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
	</select>

    <select id="listSiteConfigIds" resultType="long">
        /* SiteConfigRepository.listSiteConfigIds */
        SELECT DISTINCT sc_id  <include refid="Common.sortKeyList"/>
        FROM nv_site_config
        <include refid="siteConfigCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
    </select>


</mapper>

