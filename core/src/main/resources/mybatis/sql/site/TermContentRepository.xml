<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/Mybatis.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.site.repository.TermContentRepository">
    <!--
        <include refid="columns" /> 대신 <include refid="columns"><property name="_alias" value="b."/></include> 방식으로 alias 를 줄 수 있습니다.
    -->
    <resultMap id="simpleTermContentResult" type="termContent" extends="com.neofect.gts.core.services.site.repository.TermContentRepository_.simpleTermContentResult_">

    </resultMap>

    <sql id="termContentCondition">
        <where>
            <include refid="com.neofect.gts.core.services.site.repository.TermContentRepository_.conditions_" />
        </where>
    </sql>

    <sql id="columns">
        <include refid="com.neofect.gts.core.services.site.repository.TermContentRepository_.columns_" />
    </sql>

    <select id="getTermContent" parameterType="long" resultMap="simpleTermContentResult">
        /* TermContentRepository.getTermContent */

        SELECT
            <include refid="columns" />
        FROM
            nv_term_content
        WHERE
        tc_code = #{code}
        AND tc_revision = #{revision}
	</select>

    <select id="listTermContentCnt" resultType="int">
        /* TermContentRepository.listTermContentCnt */

        SELECT COUNT(DISTINCT tc_code tc_revision )
        FROM nv_term_content
        <include refid="termContentCondition" />
    </select>

    <select id="listTermContent" resultMap="simpleTermContentResult">
        /* TermContentRepository.listTermContent */
        SELECT
            <include refid="columns" />
        FROM nv_term_content
        <include refid="termContentCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
	</select>

    <select id="listTermContentIds" resultType="long">
        /* TermContentRepository.listTermContentIds */
        SELECT DISTINCT tc_code tc_revision  <include refid="Common.sortKeyList"/>
        FROM nv_term_content
        <include refid="termContentCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
    </select>


</mapper>

