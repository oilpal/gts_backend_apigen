<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/Mybatis.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.site.repository.TermRepository">
    <!--
        <include refid="columns" /> 대신 <include refid="columns"><property name="_alias" value="b."/></include> 방식으로 alias 를 줄 수 있습니다.
    -->
    <resultMap id="simpleTermResult" type="term" extends="com.neofect.gts.core.services.site.repository.TermRepository_.simpleTermResult_">

    </resultMap>

    <sql id="termCondition">
        <where>
            <include refid="com.neofect.gts.core.services.site.repository.TermRepository_.conditions_" />
        </where>
    </sql>

    <sql id="columns">
        <include refid="com.neofect.gts.core.services.site.repository.TermRepository_.columns_" />
    </sql>

    <select id="getTerm" parameterType="long" resultMap="simpleTermResult">
        /* TermRepository.getTerm */

        SELECT
            <include refid="columns" />
        FROM
            nv_term
        WHERE
        term_code = #{id}
	</select>

    <select id="listTermCnt" resultType="int">
        /* TermRepository.listTermCnt */

        SELECT COUNT(DISTINCT term_code )
        FROM nv_term
        <include refid="termCondition" />
    </select>

    <select id="listTerm" resultMap="simpleTermResult">
        /* TermRepository.listTerm */
        SELECT
            <include refid="columns" />
        FROM nv_term
        <include refid="termCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
	</select>

    <select id="listTermIds" resultType="long">
        /* TermRepository.listTermIds */
        SELECT DISTINCT term_code  <include refid="Common.sortKeyList"/>
        FROM nv_term
        <include refid="termCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter"/>
    </select>

    <update id="markAsDelete">
        UPDATE nv_term
        SET
            term_deleted = true
        WHERE
            term_code = #{id}
    </update>

</mapper>

