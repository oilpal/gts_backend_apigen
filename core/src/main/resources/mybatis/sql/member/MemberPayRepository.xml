<?xml version="1.0" encoding="UTF-8"?>
<!--
 ! Source code generated by UnvUS.
 ! Copyright(c) 2017 unvus.com All rights reserved.
 ! Template skrull-pack-mybatis:src/main/resources/mybatis/sql/Mybatis.e.vm.xml
 ! Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.neofect.gts.core.services.member.repository.MemberPayRepository">
    <!--
        <include refid="columns" /> 대신 <include refid="columns"><property name="_alias" value="b."/></include> 방식으로 alias 를 줄 수 있습니다.
    -->
    <resultMap id="simpleMemberPayResult" type="memberPay" extends="com.neofect.gts.core.services.member.repository.MemberPayRepository_.simpleMemberPayResult_">

    </resultMap>

    <sql id="memberPayCondition">
        <where>
            <include refid="com.neofect.gts.core.services.member.repository.MemberPayRepository_.conditions_" />
        </where>
    </sql>

    <sql id="columns">
        <include refid="com.neofect.gts.core.services.member.repository.MemberPayRepository_.columns_" />
    </sql>

    <select id="getMemberPay" parameterType="long" resultMap="simpleMemberPayResult">
        /* com.neofect.gts.core.services.member.repository.MemberPayRepository.getMemberPay */

        SELECT
            <include refid="columns" />
        FROM
            GTS2016.MEMBER_PAY
        WHERE
        MEMBER_ID = #{memberId}
        AND YYYYMM = #{yyyymm}
        AND PAY_DIV = #{payDiv}
	</select>

    <select id="listMemberPayCnt" resultType="int">
        /* com.neofect.gts.core.services.member.repository.MemberPayRepository.listMemberPayCnt */

        SELECT COUNT(DISTINCT MEMBER_ID YYYYMM PAY_DIV )
        FROM GTS2016.MEMBER_PAY
        <include refid="memberPayCondition" />
    </select>

    <select id="listMemberPay" resultMap="simpleMemberPayResult">
        /* com.neofect.gts.core.services.member.repository.MemberPayRepository.listMemberPay */
        <include refid="Common.pagingHeader-oracle"/>
        SELECT
            <include refid="columns" />
        FROM GTS2016.MEMBER_PAY
        <include refid="memberPayCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter-oracle"/>
	</select>

    <select id="listMemberPayIds" resultType="long">
        /* com.neofect.gts.core.services.member.repository.MemberPayRepository.listMemberPayIds */
        <include refid="Common.pagingHeader-oracle"/>
        SELECT DISTINCT MEMBER_ID YYYYMM PAY_DIV  <include refid="Common.sortKeyList"/>
        FROM GTS2016.MEMBER_PAY
        <include refid="memberPayCondition" />
        <include refid="Common.sort"/>
        <include refid="Common.pagingFooter-oracle"/>
    </select>


</mapper>

