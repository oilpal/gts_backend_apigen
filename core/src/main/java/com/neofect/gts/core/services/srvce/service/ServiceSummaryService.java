/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/service/Service.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.core.services.srvce.service;

import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.neofect.gts.core.services.srvce.domain.ServiceSummary;
import com.neofect.gts.core.services.srvce.repository.ServiceSummaryRepository;
import com.unvus.config.mybatis.pagination.OrderBy;
import com.unvus.query.QueryTool;


/**
 * Service for {@link ServiceSummary}<br/>
 */
@Service
public class ServiceSummaryService {

    private final ServiceSummaryRepository serviceSummaryRepository;

    public ServiceSummaryService(ServiceSummaryRepository serviceSummaryRepository) {
        this.serviceSummaryRepository = serviceSummaryRepository;
    }

    //region ------------------------------------------------------------------------------------------------ serviceSummary CRUD

    @Transactional
    public ServiceSummary saveServiceSummary(ServiceSummary serviceSummary) {
        // if (serviceSummary.getId() == null) {
        //     addServiceSummary(serviceSummary);
        // } else {
        //     modifyServiceSummary(serviceSummary);
        // }

        return serviceSummary;
    }

    private int addServiceSummary(ServiceSummary serviceSummary) {
        int result = serviceSummaryRepository.insertServiceSummary(serviceSummary);
        return result;
    }

    private int modifyServiceSummary(ServiceSummary serviceSummary) {
        int result = serviceSummaryRepository.updateServiceSummary(serviceSummary);
        return result;
    }

    @Transactional
    public int removeServiceSummary(Long id) {
        int rows = serviceSummaryRepository.deleteServiceSummary(id);
        return rows;
    }

    public ServiceSummary getServiceSummary(Long id) {
        ServiceSummary serviceSummary = serviceSummaryRepository.getServiceSummary(id);

        return serviceSummary;
    }

    public int listServiceSummaryCnt(Map<String, Object> params) {
        int rows = serviceSummaryRepository.listServiceSummaryCnt(params);
        return rows;
    }

    public List<ServiceSummary> listServiceSummary(Map<String, Object> params) {
        List<ServiceSummary> lists = serviceSummaryRepository.listServiceSummary(params);

        return lists;
    }

    public List<ServiceSummary> listServiceSummaryByIds(List<Long> ids, Map<String, Object> params) {
        params.put("ids", ids);
        QueryTool.setDefaultOrderBy(new OrderBy("CUST_ID", ids));
        QueryTool.setDefaultOrderBy(new OrderBy("DEPT_ID", ids));
        QueryTool.setDefaultOrderBy(new OrderBy("YYYYMM", ids));
        QueryTool.setDefaultOrderBy(new OrderBy("SEQ", ids));
        List<ServiceSummary> lists = serviceSummaryRepository.listServiceSummary(params);

        return lists;
    }

    //endregion

}
