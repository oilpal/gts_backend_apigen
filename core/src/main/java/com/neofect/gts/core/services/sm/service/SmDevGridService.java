/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/service/Service.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.core.services.sm.service;

import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.neofect.gts.core.services.sm.domain.SmDevGrid;
import com.neofect.gts.core.services.sm.repository.SmDevGridRepository;
import com.unvus.config.mybatis.pagination.OrderBy;
import com.unvus.query.QueryTool;


/**
 * Service for {@link SmDevGrid}<br/>
 */
@Service
public class SmDevGridService {

    private final SmDevGridRepository smDevGridRepository;

    public SmDevGridService(SmDevGridRepository smDevGridRepository) {
        this.smDevGridRepository = smDevGridRepository;
    }

    //region ------------------------------------------------------------------------------------------------ smDevGrid CRUD

    @Transactional
    public SmDevGrid saveSmDevGrid(SmDevGrid smDevGrid) {
        // if (smDevGrid.getId() == null) {
        //     addSmDevGrid(smDevGrid);
        // } else {
        //     modifySmDevGrid(smDevGrid);
        // }

        return smDevGrid;
    }

    private int addSmDevGrid(SmDevGrid smDevGrid) {
        int result = smDevGridRepository.insertSmDevGrid(smDevGrid);
        return result;
    }

    private int modifySmDevGrid(SmDevGrid smDevGrid) {
        int result = smDevGridRepository.updateSmDevGrid(smDevGrid);
        return result;
    }

    @Transactional
    public int removeSmDevGrid(Long id) {
        int rows = smDevGridRepository.deleteSmDevGrid(id);
        return rows;
    }

    public SmDevGrid getSmDevGrid(Long id) {
        SmDevGrid smDevGrid = smDevGridRepository.getSmDevGrid(id);

        return smDevGrid;
    }

    public int listSmDevGridCnt(Map<String, Object> params) {
        int rows = smDevGridRepository.listSmDevGridCnt(params);
        return rows;
    }

    public List<SmDevGrid> listSmDevGrid(Map<String, Object> params) {
        List<SmDevGrid> lists = smDevGridRepository.listSmDevGrid(params);

        return lists;
    }

    public List<SmDevGrid> listSmDevGridByIds(List<Long> ids, Map<String, Object> params) {
        params.put("ids", ids);
        QueryTool.setDefaultOrderBy(new OrderBy("SYS_ID", ids));
        QueryTool.setDefaultOrderBy(new OrderBy("PGM_CODE", ids));
        QueryTool.setDefaultOrderBy(new OrderBy("GRID_ID", ids));
        List<SmDevGrid> lists = smDevGridRepository.listSmDevGrid(params);

        return lists;
    }

    //endregion

}
