/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/service/Service.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.core.services.common.service;

import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.neofect.gts.core.services.common.domain.CodeSubul;
import com.neofect.gts.core.services.common.repository.CodeSubulRepository;
import com.unvus.config.mybatis.pagination.OrderBy;
import com.unvus.query.QueryTool;

/**
 * Service for {@link CodeSubul}<br/>
 */
@Service
public class CodeSubulService {

    private final CodeSubulRepository codeSubulRepository;

    public CodeSubulService(CodeSubulRepository codeSubulRepository) {
        this.codeSubulRepository = codeSubulRepository;
    }

    //region ------------------------------------------------------------------------------------------------ codeSubul CRUD

    @Transactional
    public CodeSubul saveCodeSubul(CodeSubul codeSubul) {
        if (codeSubul.getId() == null) {
            addCodeSubul(codeSubul);
        } else {
            modifyCodeSubul(codeSubul);
        }

        return codeSubul;
    }

    private int addCodeSubul(CodeSubul codeSubul) {
        int result = codeSubulRepository.insertCodeSubul(codeSubul);
        return result;
    }

    private int modifyCodeSubul(CodeSubul codeSubul) {
        int result = codeSubulRepository.updateCodeSubul(codeSubul);
        return result;
    }

    @Transactional
    public int removeCodeSubul(Long id) {
        int rows = codeSubulRepository.deleteCodeSubul(id);
        return rows;
    }

    public CodeSubul getCodeSubul(Long id) {
        CodeSubul codeSubul = codeSubulRepository.getCodeSubul(id);

        return codeSubul;
    }

    public int listCodeSubulCnt(Map<String, Object> params) {
        int rows = codeSubulRepository.listCodeSubulCnt(params);
        return rows;
    }

    public List<CodeSubul> listCodeSubul(Map<String, Object> params) {
        List<CodeSubul> lists = codeSubulRepository.listCodeSubul(params);

        return lists;
    }

    public List<CodeSubul> listCodeSubulByIds(List<Long> ids, Map<String, Object> params) {
        params.put("ids", ids);
        QueryTool.setDefaultOrderBy(new OrderBy("SUBUL_CODE", ids));
        List<CodeSubul> lists = codeSubulRepository.listCodeSubul(params);

        return lists;
    }

    //endregion

}
