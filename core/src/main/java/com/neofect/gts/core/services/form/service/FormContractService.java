/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/service/Service.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.core.services.form.service;

import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.neofect.gts.core.services.form.domain.FormContract;
import com.neofect.gts.core.services.form.repository.FormContractRepository;
import com.unvus.config.mybatis.pagination.OrderBy;
import com.unvus.query.QueryTool;


/**
 * Service for {@link FormContract}<br/>
 */
@Service
public class FormContractService {

    private final FormContractRepository formContractRepository;

    public FormContractService(FormContractRepository formContractRepository) {
        this.formContractRepository = formContractRepository;
    }

    //region ------------------------------------------------------------------------------------------------ formContract CRUD

    @Transactional
    public FormContract saveFormContract(FormContract formContract) {
        if (formContract.getId() == null) {
            addFormContract(formContract);
        } else {
            modifyFormContract(formContract);
        }

        return formContract;
    }

    private int addFormContract(FormContract formContract) {
        int result = formContractRepository.insertFormContract(formContract);
        return result;
    }

    private int modifyFormContract(FormContract formContract) {
        int result = formContractRepository.updateFormContract(formContract);
        return result;
    }

    @Transactional
    public int removeFormContract(Long id) {
        int rows = formContractRepository.deleteFormContract(id);
        return rows;
    }

    public FormContract getFormContract(Long id) {
        FormContract formContract = formContractRepository.getFormContract(id);

        return formContract;
    }

    public int listFormContractCnt(Map<String, Object> params) {
        int rows = formContractRepository.listFormContractCnt(params);
        return rows;
    }

    public List<FormContract> listFormContract(Map<String, Object> params) {
        List<FormContract> lists = formContractRepository.listFormContract(params);

        return lists;
    }

    public List<FormContract> listFormContractByIds(List<Long> ids, Map<String, Object> params) {
        params.put("ids", ids);
        QueryTool.setDefaultOrderBy(new OrderBy("FORM_SEQ", ids));
        List<FormContract> lists = formContractRepository.listFormContract(params);

        return lists;
    }

    //endregion

}
