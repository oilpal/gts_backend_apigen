/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/service/Service.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.core.services.srvce.service;

import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.neofect.gts.core.services.srvce.domain.ServiceContract;
import com.neofect.gts.core.services.srvce.repository.ServiceContractRepository;
import com.unvus.config.mybatis.pagination.OrderBy;
import com.unvus.query.QueryTool;


/**
 * Service for {@link ServiceContract}<br/>
 */
@Service
public class ServiceContractService {

    private final ServiceContractRepository serviceContractRepository;

    public ServiceContractService(ServiceContractRepository serviceContractRepository) {
        this.serviceContractRepository = serviceContractRepository;
    }

    //region ------------------------------------------------------------------------------------------------ serviceContract CRUD

    @Transactional
    public ServiceContract saveServiceContract(ServiceContract serviceContract) {
        // if (serviceContract.getId() == null) {
        //     addServiceContract(serviceContract);
        // } else {
        //     modifyServiceContract(serviceContract);
        // }

        return serviceContract;
    }

    private int addServiceContract(ServiceContract serviceContract) {
        int result = serviceContractRepository.insertServiceContract(serviceContract);
        return result;
    }

    private int modifyServiceContract(ServiceContract serviceContract) {
        int result = serviceContractRepository.updateServiceContract(serviceContract);
        return result;
    }

    @Transactional
    public int removeServiceContract(Long id) {
        int rows = serviceContractRepository.deleteServiceContract(id);
        return rows;
    }

    public ServiceContract getServiceContract(Long id) {
        ServiceContract serviceContract = serviceContractRepository.getServiceContract(id);

        return serviceContract;
    }

    public int listServiceContractCnt(Map<String, Object> params) {
        int rows = serviceContractRepository.listServiceContractCnt(params);
        return rows;
    }

    public List<ServiceContract> listServiceContract(Map<String, Object> params) {
        List<ServiceContract> lists = serviceContractRepository.listServiceContract(params);

        return lists;
    }

    public List<ServiceContract> listServiceContractByIds(List<Long> ids, Map<String, Object> params) {
        params.put("ids", ids);
        QueryTool.setDefaultOrderBy(new OrderBy("CUST_ID", ids));
        QueryTool.setDefaultOrderBy(new OrderBy("CONT_SEQ", ids));
        List<ServiceContract> lists = serviceContractRepository.listServiceContract(params);

        return lists;
    }

    //endregion

}
