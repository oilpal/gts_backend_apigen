/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/service/Service.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.core.services.am.service;

import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.neofect.gts.core.services.am.domain.AmCodeTaxcomp;
import com.neofect.gts.core.services.am.repository.AmCodeTaxcompRepository;
import com.unvus.config.mybatis.pagination.OrderBy;
import com.unvus.query.QueryTool;

/**
 * Service for {@link AmCodeTaxcomp}<br/>
 */
@Service
public class AmCodeTaxcompService {

    private final AmCodeTaxcompRepository amCodeTaxcompRepository;

    public AmCodeTaxcompService(AmCodeTaxcompRepository amCodeTaxcompRepository) {
        this.amCodeTaxcompRepository = amCodeTaxcompRepository;
    }

    //region ------------------------------------------------------------------------------------------------ amCodeTaxcomp CRUD

    @Transactional
    public AmCodeTaxcomp saveAmCodeTaxcomp(AmCodeTaxcomp amCodeTaxcomp) {
        // if (amCodeTaxcomp.getId() == null) {
        //     addAmCodeTaxcomp(amCodeTaxcomp);
        // } else {
        //     modifyAmCodeTaxcomp(amCodeTaxcomp);
        // }

        return amCodeTaxcomp;
    }

    private int addAmCodeTaxcomp(AmCodeTaxcomp amCodeTaxcomp) {
        int result = amCodeTaxcompRepository.insertAmCodeTaxcomp(amCodeTaxcomp);
        return result;
    }

    private int modifyAmCodeTaxcomp(AmCodeTaxcomp amCodeTaxcomp) {
        int result = amCodeTaxcompRepository.updateAmCodeTaxcomp(amCodeTaxcomp);
        return result;
    }

    @Transactional
    public int removeAmCodeTaxcomp(Long id) {
        int rows = amCodeTaxcompRepository.deleteAmCodeTaxcomp(id);
        return rows;
    }

    public AmCodeTaxcomp getAmCodeTaxcomp(Long id) {
        AmCodeTaxcomp amCodeTaxcomp = amCodeTaxcompRepository.getAmCodeTaxcomp(id);

        return amCodeTaxcomp;
    }

    public int listAmCodeTaxcompCnt(Map<String, Object> params) {
        int rows = amCodeTaxcompRepository.listAmCodeTaxcompCnt(params);
        return rows;
    }

    public List<AmCodeTaxcomp> listAmCodeTaxcomp(Map<String, Object> params) {
        List<AmCodeTaxcomp> lists = amCodeTaxcompRepository.listAmCodeTaxcomp(params);

        return lists;
    }

    public List<AmCodeTaxcomp> listAmCodeTaxcompByIds(List<Long> ids, Map<String, Object> params) {
        params.put("ids", ids);
        QueryTool.setDefaultOrderBy(new OrderBy("COMPANY_CODE", ids));
        QueryTool.setDefaultOrderBy(new OrderBy("TAX_COMP_CODE", ids));
        List<AmCodeTaxcomp> lists = amCodeTaxcompRepository.listAmCodeTaxcomp(params);

        return lists;
    }

    //endregion

}
