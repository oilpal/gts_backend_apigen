/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/service/Service.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.core.services.common.service;

import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.neofect.gts.core.services.common.domain.CodeClosing;
import com.neofect.gts.core.services.common.repository.CodeClosingRepository;
import com.unvus.config.mybatis.pagination.OrderBy;
import com.unvus.query.QueryTool;

/**
 * Service for {@link CodeClosing}<br/>
 */
@Service
public class CodeClosingService {

    private final CodeClosingRepository codeClosingRepository;

    public CodeClosingService(CodeClosingRepository codeClosingRepository) {
        this.codeClosingRepository = codeClosingRepository;
    }

    //region ------------------------------------------------------------------------------------------------ codeClosing CRUD

    @Transactional
    public CodeClosing saveCodeClosing(CodeClosing codeClosing) {
        // if (codeClosing.getId() == null) {
        //     addCodeClosing(codeClosing);
        // } else {
        //     modifyCodeClosing(codeClosing);
        // }

        return codeClosing;
    }

    private int addCodeClosing(CodeClosing codeClosing) {
        int result = codeClosingRepository.insertCodeClosing(codeClosing);
        return result;
    }

    private int modifyCodeClosing(CodeClosing codeClosing) {
        int result = codeClosingRepository.updateCodeClosing(codeClosing);
        return result;
    }

    @Transactional
    public int removeCodeClosing(Long id) {
        int rows = codeClosingRepository.deleteCodeClosing(id);
        return rows;
    }

    public CodeClosing getCodeClosing(Long id) {
        CodeClosing codeClosing = codeClosingRepository.getCodeClosing(id);

        return codeClosing;
    }

    public int listCodeClosingCnt(Map<String, Object> params) {
        int rows = codeClosingRepository.listCodeClosingCnt(params);
        return rows;
    }

    public List<CodeClosing> listCodeClosing(Map<String, Object> params) {
        List<CodeClosing> lists = codeClosingRepository.listCodeClosing(params);

        return lists;
    }

    public List<CodeClosing> listCodeClosingByIds(List<Long> ids, Map<String, Object> params) {
        params.put("ids", ids);
        QueryTool.setDefaultOrderBy(new OrderBy("YYYYMM", ids));
        QueryTool.setDefaultOrderBy(new OrderBy("DEPT_ID", ids));
        List<CodeClosing> lists = codeClosingRepository.listCodeClosing(params);

        return lists;
    }

    //endregion

}
