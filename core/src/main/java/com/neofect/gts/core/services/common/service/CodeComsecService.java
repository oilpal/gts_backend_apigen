/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/service/Service.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.core.services.common.service;

import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.neofect.gts.core.services.common.domain.CodeComsec;
import com.neofect.gts.core.services.common.repository.CodeComsecRepository;
import com.unvus.config.mybatis.pagination.OrderBy;
import com.unvus.query.QueryTool;

/**
 * Service for {@link CodeComsec}<br/>
 */
@Service
public class CodeComsecService {

    private final CodeComsecRepository codeComsecRepository;

    public CodeComsecService(CodeComsecRepository codeComsecRepository) {
        this.codeComsecRepository = codeComsecRepository;
    }

    //region ------------------------------------------------------------------------------------------------ codeComsec CRUD

    @Transactional
    public CodeComsec saveCodeComsec(CodeComsec codeComsec) {
        if (codeComsec.getId() == null) {
            addCodeComsec(codeComsec);
        } else {
            modifyCodeComsec(codeComsec);
        }

        return codeComsec;
    }

    private int addCodeComsec(CodeComsec codeComsec) {
        int result = codeComsecRepository.insertCodeComsec(codeComsec);
        return result;
    }

    private int modifyCodeComsec(CodeComsec codeComsec) {
        int result = codeComsecRepository.updateCodeComsec(codeComsec);
        return result;
    }

    @Transactional
    public int removeCodeComsec(Long id) {
        int rows = codeComsecRepository.deleteCodeComsec(id);
        return rows;
    }

    public CodeComsec getCodeComsec(Long id) {
        CodeComsec codeComsec = codeComsecRepository.getCodeComsec(id);

        return codeComsec;
    }

    public int listCodeComsecCnt(Map<String, Object> params) {
        int rows = codeComsecRepository.listCodeComsecCnt(params);
        return rows;
    }

    public List<CodeComsec> listCodeComsec(Map<String, Object> params) {
        List<CodeComsec> lists = codeComsecRepository.listCodeComsec(params);

        return lists;
    }

    public List<CodeComsec> listCodeComsecByIds(List<Long> ids, Map<String, Object> params) {
        params.put("ids", ids);
        QueryTool.setDefaultOrderBy(new OrderBy("COMSEC_CODE", ids));
        List<CodeComsec> lists = codeComsecRepository.listCodeComsec(params);

        return lists;
    }

    //endregion

}
