/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/service/Service.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.core.services.am.service;

import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.neofect.gts.core.services.am.domain.AmCodeRate;
import com.neofect.gts.core.services.am.repository.AmCodeRateRepository;
import com.unvus.config.mybatis.pagination.OrderBy;
import com.unvus.query.QueryTool;

/**
 * Service for {@link AmCodeRate}<br/>
 */
@Service
public class AmCodeRateService {

    private final AmCodeRateRepository amCodeRateRepository;

    public AmCodeRateService(AmCodeRateRepository amCodeRateRepository) {
        this.amCodeRateRepository = amCodeRateRepository;
    }

    //region ------------------------------------------------------------------------------------------------ amCodeRate CRUD

    @Transactional
    public AmCodeRate saveAmCodeRate(AmCodeRate amCodeRate) {
        if (amCodeRate.getId() == null) {
            addAmCodeRate(amCodeRate);
        } else {
            modifyAmCodeRate(amCodeRate);
        }

        return amCodeRate;
    }

    private int addAmCodeRate(AmCodeRate amCodeRate) {
        int result = amCodeRateRepository.insertAmCodeRate(amCodeRate);
        return result;
    }

    private int modifyAmCodeRate(AmCodeRate amCodeRate) {
        int result = amCodeRateRepository.updateAmCodeRate(amCodeRate);
        return result;
    }

    @Transactional
    public int removeAmCodeRate(Long id) {
        int rows = amCodeRateRepository.deleteAmCodeRate(id);
        return rows;
    }

    public AmCodeRate getAmCodeRate(Long id) {
        AmCodeRate amCodeRate = amCodeRateRepository.getAmCodeRate(id);

        return amCodeRate;
    }

    public int listAmCodeRateCnt(Map<String, Object> params) {
        int rows = amCodeRateRepository.listAmCodeRateCnt(params);
        return rows;
    }

    public List<AmCodeRate> listAmCodeRate(Map<String, Object> params) {
        List<AmCodeRate> lists = amCodeRateRepository.listAmCodeRate(params);

        return lists;
    }

    public List<AmCodeRate> listAmCodeRateByIds(List<Long> ids, Map<String, Object> params) {
        params.put("ids", ids);
        QueryTool.setDefaultOrderBy(new OrderBy("CONTENTS_YEAR", ids));
        List<AmCodeRate> lists = amCodeRateRepository.listAmCodeRate(params);

        return lists;
    }

    //endregion

}
