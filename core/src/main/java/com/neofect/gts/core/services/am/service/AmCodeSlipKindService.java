/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/service/Service.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.core.services.am.service;

import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.neofect.gts.core.services.am.domain.AmCodeSlipKind;
import com.neofect.gts.core.services.am.repository.AmCodeSlipKindRepository;
import com.unvus.config.mybatis.pagination.OrderBy;
import com.unvus.query.QueryTool;

/**
 * Service for {@link AmCodeSlipKind}<br/>
 */
@Service
public class AmCodeSlipKindService {

    private final AmCodeSlipKindRepository amCodeSlipKindRepository;

    public AmCodeSlipKindService(AmCodeSlipKindRepository amCodeSlipKindRepository) {
        this.amCodeSlipKindRepository = amCodeSlipKindRepository;
    }

    //region ------------------------------------------------------------------------------------------------ amCodeSlipKind CRUD

    @Transactional
    public AmCodeSlipKind saveAmCodeSlipKind(AmCodeSlipKind amCodeSlipKind) {
        if (amCodeSlipKind.getId() == null) {
            addAmCodeSlipKind(amCodeSlipKind);
        } else {
            modifyAmCodeSlipKind(amCodeSlipKind);
        }

        return amCodeSlipKind;
    }

    private int addAmCodeSlipKind(AmCodeSlipKind amCodeSlipKind) {
        int result = amCodeSlipKindRepository.insertAmCodeSlipKind(amCodeSlipKind);
        return result;
    }

    private int modifyAmCodeSlipKind(AmCodeSlipKind amCodeSlipKind) {
        int result = amCodeSlipKindRepository.updateAmCodeSlipKind(amCodeSlipKind);
        return result;
    }

    @Transactional
    public int removeAmCodeSlipKind(Long id) {
        int rows = amCodeSlipKindRepository.deleteAmCodeSlipKind(id);
        return rows;
    }

    public AmCodeSlipKind getAmCodeSlipKind(Long id) {
        AmCodeSlipKind amCodeSlipKind = amCodeSlipKindRepository.getAmCodeSlipKind(id);

        return amCodeSlipKind;
    }

    public int listAmCodeSlipKindCnt(Map<String, Object> params) {
        int rows = amCodeSlipKindRepository.listAmCodeSlipKindCnt(params);
        return rows;
    }

    public List<AmCodeSlipKind> listAmCodeSlipKind(Map<String, Object> params) {
        List<AmCodeSlipKind> lists = amCodeSlipKindRepository.listAmCodeSlipKind(params);

        return lists;
    }

    public List<AmCodeSlipKind> listAmCodeSlipKindByIds(List<Long> ids, Map<String, Object> params) {
        params.put("ids", ids);
        QueryTool.setDefaultOrderBy(new OrderBy("SLIP_KIND_CODE", ids));
        List<AmCodeSlipKind> lists = amCodeSlipKindRepository.listAmCodeSlipKind(params);

        return lists;
    }

    //endregion

}
