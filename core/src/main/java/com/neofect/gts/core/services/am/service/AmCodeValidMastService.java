/*
 * Source code generated by UnvUS.
 * Copyright(c) 2017 unvus.com All rights reserved.
 * Template skrull-pack-mybatis:src/main/java/service/Service.e.vm.java
 * Template is part of project: https://git.unvus.com/unvus/opensource/pack-unvus-mybatis
 */
package com.neofect.gts.core.services.am.service;

import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.neofect.gts.core.services.am.domain.AmCodeValidMast;
import com.neofect.gts.core.services.am.repository.AmCodeValidMastRepository;
import com.unvus.config.mybatis.pagination.OrderBy;
import com.unvus.query.QueryTool;

/**
 * Service for {@link AmCodeValidMast}<br/>
 */
@Service
public class AmCodeValidMastService {

    private final AmCodeValidMastRepository amCodeValidMastRepository;

    public AmCodeValidMastService(AmCodeValidMastRepository amCodeValidMastRepository) {
        this.amCodeValidMastRepository = amCodeValidMastRepository;
    }

    //region ------------------------------------------------------------------------------------------------ amCodeValidMast CRUD

    @Transactional
    public AmCodeValidMast saveAmCodeValidMast(AmCodeValidMast amCodeValidMast) {
        if (amCodeValidMast.getId() == null) {
            addAmCodeValidMast(amCodeValidMast);
        } else {
            modifyAmCodeValidMast(amCodeValidMast);
        }

        return amCodeValidMast;
    }

    private int addAmCodeValidMast(AmCodeValidMast amCodeValidMast) {
        int result = amCodeValidMastRepository.insertAmCodeValidMast(amCodeValidMast);
        return result;
    }

    private int modifyAmCodeValidMast(AmCodeValidMast amCodeValidMast) {
        int result = amCodeValidMastRepository.updateAmCodeValidMast(amCodeValidMast);
        return result;
    }

    @Transactional
    public int removeAmCodeValidMast(Long id) {
        int rows = amCodeValidMastRepository.deleteAmCodeValidMast(id);
        return rows;
    }

    public AmCodeValidMast getAmCodeValidMast(Long id) {
        AmCodeValidMast amCodeValidMast = amCodeValidMastRepository.getAmCodeValidMast(id);

        return amCodeValidMast;
    }

    public int listAmCodeValidMastCnt(Map<String, Object> params) {
        int rows = amCodeValidMastRepository.listAmCodeValidMastCnt(params);
        return rows;
    }

    public List<AmCodeValidMast> listAmCodeValidMast(Map<String, Object> params) {
        List<AmCodeValidMast> lists = amCodeValidMastRepository.listAmCodeValidMast(params);

        return lists;
    }

    public List<AmCodeValidMast> listAmCodeValidMastByIds(List<Long> ids, Map<String, Object> params) {
        params.put("ids", ids);
        QueryTool.setDefaultOrderBy(new OrderBy("VALIDATION_CODE", ids));
        List<AmCodeValidMast> lists = amCodeValidMastRepository.listAmCodeValidMast(params);

        return lists;
    }

    //endregion

}
